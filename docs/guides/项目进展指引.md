# AIgo 项目进展指引（2026-09-10 更新）

> 本文件是项目当前状态的完整快照，供新的 AI 对话/开发者快速上手。
> 产品基准永远是根目录《功能需求文档.txt》和《CLAUDE.md》，冲突时以它们为准。

## 1. 项目一句话定位

面向国家执业医师考试 **A2 型试题**的 AI 辅助命题系统：
知识点 Excel / 大纲 → 千问出题（草稿）→ AI 质量检查（可选）→ 多轮人工审核（可配置）→ 最终把关通过，支持 xlsx/docx 导出。

当前题型统一为纯文本 A2 单选题，不再保留题目附图能力。

## 2. 当前状态速览

- **Git**：分支 `codex/navigation-and-batch-workspace`，当前 commit `ac0ca35`，已上传到对应远程分支；上传脚本不会自动提交或推送
- **代码健康**：`go test ./...`、`npm run build`、前端导航测试、`git diff --check` 均通过
- **本地服务状态**：本地开发服务当前可按需启动；服务器生产服务正在运行
- **本地数据库**：PostgreSQL@16（brew 安装），本地迁移版本 22，数据目录 `/opt/homebrew/var/postgresql@16`
- **服务器数据库**：PostgreSQL 16 容器，迁移版本 22；业务数据使用 Docker 持久卷，重建容器不会删除数据
- **题库数据**：约 5262 道题（历史题已恢复为全局正式/待审核/淘汰三层，另含管理员个人题），知识点 7235 个；题目可按专业（18 个专业值）筛选
- **审核数据（测试痕迹，保留）**：旧版 `approved` 生命周期已迁移，题目和任务唯一成功终态为 `published`（界面显示“已通过”）；专家投票结论仍使用 `approved`
- **权限体系**：角色模板（唯一 super_admin、可下放 admin、expert/teacher + 自定义）+ 用户直接勾选24个权限点 + 题库范围；单题出题（`question:generate`）与批量推理（`batch:run`）独立分配，默认仅 super_admin 继承批量推理；个人题库按用户隔离，正式题目可一次申请分享至全局三层题库；自助注册默认开启，新用户为空角色/零业务权限/空题库范围，管理员自动可见并负责授权；提交审核仅管理员（user:manage）
- **AI 质量检查口径**：只对医学内容、答案解析一致性、选项逻辑和 A2 格式做硬性判断；难度、认知层次、考核要点等元数据只输出为出题提示词反馈，不得因元数据标签不一致自动淘汰内容正确且可用的题目
- **题库自动归纳**：题库可指定专业范围（professions），创建/「重新归纳」时自动收纳匹配专业的未分类题目；AI 出题/新建题目自动归入第一个匹配题库；可在题库详情手动移入/移出微调
- **批量提交审核**：审核流程配置页可一键把题库内所有可提交题目统一提交（`POST /api/review/submit-bank`），审核页保留用于查看状态
- **审核结果页**：`/review-results` 统计（通过/驳回/需修改/待决断等）+ 找出问题题目 + 每题的专家评语与最终决断
- **题库导出**：题库页按 100 道页码分页，支持跨页勾选和全选全部筛选结果；Excel/Word 只导出 `published/已通过` 题目，并记录导出审计
- **列表工作区**：题库、AI 检查左栏采用固定搜索/操作区 + 独立纵向题目列表；题库卡片的状态/删除使用固定操作列，不受长题干裁剪影响；1280px 常用桌面宽度和 800px 窄屏均不再出现内容区横向拖拽
- **固定分页**：题库、知识点主表、知识点选择器、题库管理添加结果均采用“中间列表滚动 + 底部分页固定”，翻页后自动回到列表顶部
- **健康检查**：`GET /health/live` 仅检查进程存活；`GET /health/ready` 以 2 秒超时检查 PostgreSQL 与关键 Schema，不暴露 DSN/SQL 错误；`start.sh` 使用 readiness 判断后端启动成功
- **版本化迁移**：`schema_migrations` 保存版本、名称、SHA-256 校验和和应用时间；`go run ./cmd/aigo migrate` 独立升级，事务回滚 + PostgreSQL advisory lock 保证失败与并发安全；其他命令拒绝在未迁移、断档、过新或校验和不一致的数据库上运行
- **正式运行**：`build-production.sh` 生成Go二进制 + Vue静态资源的不可变版本release，`output/production` 指向当前版本；`run-production.sh` 以前台单进程同源提供页面和API，默认开启低权限自助注册、禁用项目 `.env`，运行阶段不需要Node/Vite，也不管理外部PostgreSQL
- **容器部署**：`Dockerfile + compose.production.yaml + deploy/` 已提供单机构部署骨架；应用非 root/只读根文件系统，PostgreSQL/输出/证书使用持久卷，迁移为一次性服务，Caddy 负责 HTTPS 和 JSON 访问日志，Secrets 以文件挂载
- **备份恢复**：独立非 root 备份服务立即+定时生成 custom archive，保存 SHA-256、版本元数据和稳定核心表指纹，按天数/份数清理；`deploy/backup.sh` 支持创建、校验、恢复演练、新库恢复和宿主机导出，禁止覆盖当前业务库
- **异机加密备份**：可选 `compose.offsite.yaml` 使用 `age` 公钥加密备份三件套并以不可覆盖模式上传 S3 兼容存储；上传后完整回读校验、receipt 最后提交，支持幂等扫描、状态重建、断网恢复、远端校验/下载和离线私钥恢复，主应用不保存私钥且不依赖该服务启动
- **账号防滥用**：Schema 3持久化独立账号/IP登录桶和注册IP桶；错误登录通用响应、短时指数限速、脱敏失败审计和dummy bcrypt防账号枚举；仅Compose内受控Caddy的客户端IP头可信

### 2.1 2026-09-10 ECS 部署与访问快照（新对话优先阅读）

#### 服务器与目录

- **云服务器**：阿里云 ECS，Ubuntu 24.04 LTS，公网 IP `123.56.164.1`
- **服务器项目目录**：`/opt/aigo`
- **本地项目目录**：`/Users/imaichika/Documents/AIgo`
- **服务器部署方式**：Docker Compose；服务器目录是部署副本，不是 Git 工作树，通常通过本地同步源码后重新构建
- **生产配置**：`/opt/aigo/deploy/production.env`（不提交 Git）
- **服务器密钥目录**：`/opt/aigo/deploy/secrets/`（不提交 Git）
- **容器**：`aigo-postgres-1`、`aigo-app-1`、`aigo-caddy-1`、`aigo-backup-1`；迁移容器执行成功后退出是正常现象

#### 访问方式与安全组

- **网站入口**：`http://123.56.164.1/login`
- **当前 HTTPS 状态**：尚未绑定域名，当前站点地址为 HTTP；绑定域名并配置证书后，再把 `AIGO_SITE_ADDRESS` 改为 `https://域名`
- **公网端口**：TCP 80、443；应用 8080 和 PostgreSQL 5432 不对公网发布
- **SSH**：TCP 22，来源应填写办公公网 IP/32；`123.56.164.1` 是 ECS 目的地址，不是 SSH 安全组来源
- **健康检查**：`GET http://123.56.164.1/health/live` 与 `/health/ready` 均应返回 200
- **SSH 密码**：不写入本文件、不写入 Git；由用户安全保存，遗失时在阿里云控制台重置
- **Web 超级管理员**：用户名为 `admin`；密码不写入本文件，建议 HTTPS 启用后立即修改为至少 8 位强密码
- **Qwen API Key**：存于数据库加密配置，不写代码、`.env`、文档或 Git；Web “AI 服务配置”仅超级管理员可修改

#### 当前服务器功能状态

- **实时 AI**：当前激活配置为云端 Qwen，地址为百炼 OpenAI 兼容接口，生成模型和 AI 检查模型均为 `qwen3.5-flash`
- **批量推理**：Web 端固定为 `simulation`，只模拟提交、进度、完成和导入，不调用外部 Batch API、不写入真实题目
- **知识点**：本地 `data/knowledge/知识点表.xlsx` 已通过网页导入服务器，共 516 条，位于“历史大纲（年份待确认）”版本；旧版五列表格格式已兼容
- **知识点网页功能**：支持网页批量导入，以及“导出当前版本 Excel”；导出复测为 516 行且浏览器无控制台错误
- **Qwen 实测**：使用导入的 `KP-001` 成功生成 1 道题，AI 检查通过，状态为 `ai_reviewed`
- **角色模板**：用户管理页面不再显示 `super_admin` 可分配选项；现有唯一超级管理员显示为只读标签；后端构造请求分配该角色会返回 403

#### 后续开发与同步流程

1. 在本地 `/Users/imaichika/Documents/AIgo` 修改代码，不直接在 `/opt/aigo` 改业务源码。
2. 本地验证：

   ```bash
   go test ./...
   cd web && npm run build && cd ..
   ```

3. 提交并上传当前分支：

   ```bash
   git status
   git add <需要提交的文件>
   git commit -m "描述本次修改"
   bash tools/push_branch.sh --dry-run
   bash tools/push_branch.sh
   ```

4. 同步到服务器后，在服务器执行：

   ```bash
   cd /opt/aigo
   ./deploy/compose.sh up -d --build
   curl -f http://127.0.0.1/health/live
   curl -f http://127.0.0.1/health/ready
   ```

5. **禁止**使用 `docker compose down -v`，否则会删除数据库/备份卷；只重建应用容器时使用上述 Compose 命令即可。

6. 当前服务器同步通常通过 SSH/rsync 完成，排除 `.git`、`.env`、`data/`、`output/`、`deploy/secrets/` 和本地截图；服务器不会自动跟随 Git 分支更新。

#### 已解决的部署问题

- 首次启动时 8080 被旧进程占用：`start.sh` 已加入端口占用检查和健康复用逻辑
- Go 依赖下载超时：生产 Dockerfile 已配置 `GOPROXY=https://goproxy.cn,direct`
- Docker Hub 拉取不稳定：当前 ECS 配置了镜像加速；长期建议迁移到阿里云 ACR 私有镜像仓库
- Secret 权限导致迁移失败：服务器 Secret 已设置为容器用户可读的 0600 权限
- HTTP 421：反向代理本机检查必须带正确的 `Host: 123.56.164.1`；公网入口直接访问即可

## 3. 必读文件

| 文件 | 作用 |
|---|---|
| `功能需求文档.txt` | 产品需求基准（树状图） |
| `CLAUDE.md` | 开发约束（Go 为主、AI 结果默认草稿、不做知识图谱等） |
| `docs/guides/项目结构.txt` | 目录结构和阅读顺序 |
| `docs/analysis/problem.txt` | 历史 bug 清单（已大部分修复） |
| **本文件** | 项目进展指引 |

## 4. 技术栈

- **后端**：Go 1.22+（net/http 标准库路由 `method /path/{param}`），仅依赖 `lib/pq`、`excelize`
- **前端**：Vue 3 + Vite 7 + vue-router（Element Plus 未实际使用）；开发模式端口 5173 并代理 `/api`，正式构建由 Go 在同一地址托管
- **数据库**：PostgreSQL 16，基线 Schema 内嵌到后端；正式迁移由 `internal/storage/postgres/migrations.go` 管理，`start.sh` 先执行独立 `aigo migrate`，后端启动阶段只检查版本
- **LLM**：千问 OpenAI 兼容接口；实时生成/AI 检查配置由超级管理员在 Web “AI 服务配置”维护并加密落库，环境变量仅用于首次兼容导入/回退；当前可使用百炼 `qwen3.5-flash`，也保留本地 Qwen 3.5 服务接入
- **本地推理状态**：`QWEN_DEPLOYMENT=local`、本地 Base URL、served-model-name 和思考模式参数已经预留；本地模型服务、GPU 运行环境、容量评测、高可用和本地持久批量队列尚未建设，不能把“接口可切换”视为“本地推理已可生产使用”
- **批量出题**：Web 管理端当前使用本地模拟执行器验证提交/轮询/导入流程；CLI `batch-run/batch-status/batch-download` 保留 DashScope JSONL 任务兼容能力
- **测试工具**：后端 go test；前端 Playwright（anaconda python3，系统 Chrome 通道）

## 5. 目录结构（当前）

```
cmd/aigo/main.go        CLI 入口（serve/import/kp-import/batch-run/export 等 30+ 命令）
internal/
  api/                  HTTP handler（按资源拆分，server.go 注册路由+权限映射）
  auth/                 JWT + 用户/角色/权限（注册、角色模板、权限矩阵、题库范围、HasPermission）
  aicheck/              AI 质量检查（四维评分 + 问题 + 建议，结果带 question_version）
  audit/                审计日志（submit/resubmit/review/publish/flow_*/expert_*/user_* 等动作）
  bank/                 题库（分库）服务：专业范围自动归纳 + 手动移入/移出微调
  batch/                DashScope 批量推理
  config/               .env + 环境变量加载（JWT_SECRET 也从 env 读）
  domain/               领域模型（题目/知识点/审核/AI检查/权限/角色/题库类型 + 状态常量）
  evaluator/            规则评估器（基本未在 Web 流程启用）
  exporter/             docx 导出（原生 OOXML 生成，标准库实现，无外部依赖）
  generator/            出题 prompt + JSON 解析（含 SanitizeIDPrefix）
  importer/             xlsx 导入（题目/知识点大纲）
  knowledge/            知识点服务
  llm/                  千问客户端
  pipeline/             编排层（CLI 用）
  review/               审核服务（SubmitQuestion/Review 投票统计/Finalize 决断/自动匹配审题人）
  storage/interfaces.go 存储接口定义
  storage/postgres/     PG 实现（schema.sql 基线 + migrations.go 版本化迁移 + store.go）
  storage/testutil/      内存实现（测试用）
web/src/
  api.js                API 封装（BASE=/api 相对路径，401 跳过登录接口，下载带 token）
  auth.js               token/用户状态（hasPerm/roleName 权限判断）
  router.js             路由 + 守卫（按 meta.perm 权限点重定向）
  App.vue               侧边栏（菜单按权限点显示）
  components/           QuestionDetailModal.vue 共享题目详情弹窗（题干/选项/答案/解析/大纲代码/知识点/元信息）
  pages/                15 个页面（Login/Generate/Knowledge/AICheck/Review/ReviewResults/Bank/Batch/Stats/ReviewFlow/Audit/Users/Roles/Banks）
configs/review_flows.json  审核流程初始配置（仅空库时导入）
start.sh / end.sh       一键启停（end.sh 已修复 launchd KeepAlive 卡死问题）
build-production.sh     lockfile 前端构建 + Go 构建 + 版本化 release 切换
run-production.sh       迁移后以前台单进程运行当前 release（供进程管理器托管）
Dockerfile / compose.production.yaml / compose.offsite.yaml  单机构容器镜像、主生产编排与可选异机备份扩展
deploy/                 Caddyfile、部署配置模板、Secret 初始化、Compose 包装脚本与部署说明
  backup-tool.sh        容器内自动备份、校验、保留、恢复与指纹比对
  backup.sh             宿主机备份/恢复/导出/解密备份导入入口
  offsite-tool.sh       容器内age加密、S3不可覆盖上传、receipt与远端回读校验
  offsite-compose.sh    主Compose+可选异机Compose的独立包装入口
  offsite.sh            宿主机上传、列出、校验和下载加密包入口
```

## 6. 核心业务流程（题目状态机）

```
ai_draft（草稿，出题/手工新建/批量导入默认）
  │  AI 检查（可选）verdict=pass 且状态为草稿态
  ▼
ai_reviewed ──────────┐
  │                   │（auto_checked：历史遗留状态，规则评估器未启用）
  │ 提交审核 SubmitQuestion（白名单：ai_draft/auto_checked/ai_reviewed/revision_required/rejected）
  │（轮次审核人：流程显式配置，或按「审题权限+题库范围」自动匹配）
  ▼
reviewing（逐轮推进，每轮按「通过票数」门槛）
  ├─ 达到通过票数 → 本轮通过（无视反对票，反对票记录供最终决断参考）：
  │     ├─ 非最终轮 → 进入下一轮（把关人不介入）
  │     └─ 最终轮 → 所有轮次完成 → conflict（最终待决断）→ 把关人最终决断
  └─ 全员投完仍未达标（分歧）→ conflict（待决断）→ 把关人决断
        ├─ approved（决断动作）→ 下一轮 / 最终轮则直接 published（已通过）
        ├─ rejected → 任务+题目 rejected（重提从头）
        └─ revision_required → 编辑（版本+1）→ 重提（本轮票清零）
（可选「一票否决」规则：任一驳回直接驳回题目，全员通过才过轮）
已 published 题目被编辑 → 自动回退 ai_draft（旧任务终态时重新提交会创建新任务）
```

**关键规则**：
- AI 检查**只能推进草稿态**，绝不能回退 reviewing/published（人工审核优先）
- 送审、投票、最终决断、流程变更和撤销由 PostgreSQL advisory lock 跨进程串行化，防止并发丢票和同题重复任务
- 送审、投票、最终决断、退修重提、撤销和发布的审核记录/任务/题目状态使用同一 PostgreSQL 事务；失败整体回滚，跨进程锁继续负责并发串行化
- 每次题目内容变化生成不可变 `question_versions` 快照；审核任务绑定 `question_version`，版本不一致时拒绝投票/决断。状态和题库归属变化不新增内容版本
- 版本 2 迁移兼容旧表的 `id` 主键、`snapshot TEXT` 和 `changed_by` 列；执行 `aigo migrate` 时原位升级为 JSONB 并补 actor/change_type/唯一约束，不删除旧记录
- reviewing/conflict 题目禁止直接编辑；必须先撤销提交或由把关人退回 revision_required 后再修改
- **每轮按「通过票数」门槛推进**（如“1人通过即可”）：达到门槛即过轮；其他意见继续留痕并供最终把关人参考
- **把关人（决断者）只在所有轮次通过后出现**（最终待决断）；中途轮次出现分歧（全员投完仍有反对票）时才提前介入决断
- 最终轮决断通过 → 直接入库；分歧轮次决断通过 → 进入下一轮
- 终态任务（rejected/published）禁止再审核；conflict 任务禁止继续投票（只能决断）
- 把关人（review:final）可审任意轮次；把关人名单按流程 `final_reviewer_ids` 快照校验
- 审核记录 ID 带时间戳，同一专家打回后可重审；决断记录同样留痕
- 提交审核写 submit/resubmit 日志，审核写 review 日志（含意见），全程留痕

## 7. 审核流程配置（可投入使用）

- 前端 `/review-flows`（flow:manage 权限）：新建/编辑/删除流程；专家在审核分组只显示「待我审核」，同时保留个人命题、待我修改和我的题库，不显示审核记录汇总和数据统计
- 流程字段：`bank_id`（适用题库，空=通用）、`final_reviewer_ids`（最终把关管理员名单，空=任意有「最终把关」权限者）、`rounds`（多轮，每轮 `expert_ids` 可为空 = 提交时按审题权限自动匹配）
- 后端校验（`review.CreateFlow/UpdateFlow`）：审核人必须是有审题权限的启用用户（或专家库历史记录兼容）、同轮去重、重复流程 ID 拒绝
- **编辑保护**：有进行中任务（reviewing/revision_required/conflict）的流程禁止修改/删除；有历史任务的流程禁止删除（保留追溯）
- 审核人选择：从「有审题权限的用户」中选择（`/api/review/reviewers`）；专家库页面已移除，由角色管理/用户管理替代
- 每轮可配置通过票数门槛和投票规则（通过票数推进/一票否决）；所有轮次完成后由把关人最终决断，中途未达标分歧会提前进入待决断

## 8. 权限体系（RBAC 可配置）

**权限模型**（`internal/domain/permission.go`，24 个权限点，提交审核为管理员专属不再设权限点）：

- 用户可属于一个**角色模板**（roles 表，管理员可自定义 n 种角色）；也可**直接勾选权限点**（users.permissions）
- **有效权限 = 角色模板权限（全范围）∪ 直接分配权限（可带题库范围 users.bank_ids，空=全部题库）**
- 内置角色分为：`super_admin`（系统唯一超级管理员，完整权限，数据库唯一索引保证只能有一个）、`admin`（超级管理员分配的业务管理员，默认拥有除角色模板管理外的业务权限）、`expert`（个人命题/题库/退修/分享 + 分配任务审核，不含全局汇总和统计）和 `teacher`（命题）
- `role:manage` 只对 `super_admin` 生效；管理员可以增删普通用户、安排审核流程、管理题库并审核/决断，但不能创建第二个超级管理员、分配管理员身份或修改角色模板
- 超级管理员账号受保护，不能通过用户管理自我降级、禁用或删除；历史库中的默认 `admin` 账号启动时自动升级为 `super_admin`
- 每个用户拥有独立的正式/待审核/淘汰个人题库；题目接口按 `owner_id` 隔离，题目详情不返回出题人字段，审计日志和版本记录保留原始操作人
- 个人正式题目通过 `/api/questions/{id}/share` 申请进入全局库：`pending` 为全局待审核、管理员审批 `approved` 后进入全局正式、`rejected` 进入全局淘汰；每题唯一申请记录，不能重复申请
- 全局题库需要 `question:view_global`；申请需要 `question:share`，审批需要 `question:share_review`。管理员默认拥有全局查看与审批权限，普通客户默认只能看到自己的个人题库
- 审核记录汇总由独立的 `review:view_results` 控制，个人题库查看 `question:view` 不再自动开放审核记录页
- 管理员的数据统计默认进入全局范围，可切换“我的数据”；个人统计与全局统计分别按题目归属/分享状态聚合，避免个人题混入全局指标
- 题库范围权限点：查看/新建/编辑/删除题目、AI 出题、题库下载、审题、最终把关、统计分析（提交审核由管理员在题库页操作）
- 全局权限点：用户管理、角色管理、题库管理、审核流程配置、专家库管理（历史兼容，无独立页面）、操作日志、批量推理、知识点管理；批量推理和单题出题是两个独立权限点
- 内置角色模板：super_admin（权限固定，不可修改/删除）、admin（可由超级管理员调整业务权限）、expert（医学专家：个人命题与任务审核）、teacher（命题教师）
- **用户自主注册**（`POST /api/auth/register`，开关启用时登录页显示入口），注册后无任何权限，管理员在「用户管理」页按**权限矩阵勾选**分配（一列列打勾），并可选题库范围
- 用户可在左下角「个人设置」验证当前密码后修改密码；旧密码立即失效
- 权限检查从数据库实时读取，权限变更立即生效；禁用账号后 token 立即失效（JWT 仍 24h 但每请求校验启用状态）

**审核人匹配规则**：
- 流程轮次 `expert_ids` 显式指定 → 按名单审核
- `expert_ids` 留空 → 提交审核时自动收集**直接勾选「审题」权限且题库范围覆盖题目所属库**的用户（如内科分配 3 位老师 → 3 位都是审核人）
- 角色模板带来的审题权限不参与自动匹配（避免 admin/专家角色被自动拉进每道题）

## 9. 审核投票与最终把关

**逐轮推进**：每轮配置「通过票数」（required_count，0=全员）。达到通过票数 → 本轮通过：

1. 非最终轮通过 → 直接进入下一轮（专家审核正常进行，把关人不介入）
2. **最终轮通过 → 所有轮次完成 → 进入最终待决断（conflict）**，把关人此时才出现
3. 分歧处理：某轮全员投完仍未达到通过票数 → 该轮进入待决断，把关人决断
4. **最终把关人决断**（`POST /api/review/finalize`，需 review:final 权限）：
   - 把关人必须在流程 `final_reviewer_ids` 名单内（名单空 = 任意把关权限者；系统管理员 user:manage 不受限）
   - 决断 approved（分歧轮）→ 进入下一轮
   - 决断 approved（最终轮）→ **直接 published（已通过）**
   - 决断 rejected → 驳回；revision_required → 退回修改（重提后本轮票清零）
5. 决断记录写入 review_records + 任务 final_decision 字段，全程留痕

**题目状态机**：

```
ai_draft / auto_checked / ai_reviewed / revision_required / rejected
        ↓ SubmitQuestion（自动匹配审核人）
reviewing ── 每轮按通过票数推进 ──┬─ 最终轮通过 → conflict（最终待决断）
                                  └─ 分歧 → conflict（待决断）
        conflict ── 把关人决断 ──┬─ approved（非最终轮）→ 下一轮 reviewing
                                ├─ approved（最终轮）→ published 已通过
                                ├─ rejected（重提从头）
                                └─ revision_required（编辑后重提，本轮清零）
```

## 10. 数据库表（20 张）

schema_migrations（版本/名称/校验和/应用时间）、auth_login_limits（账号/IP注册登录限速）、users（含 permissions/bank_ids 列）、roles（角色模板）、question_banks（含 professions 专业范围列）、question_bank_members、knowledge_points、knowledge_versions、questions、question_versions（不可变内容快照）、question_share_requests（个人正式题目一次性全局分享申请）、experts、review_flows、review_tasks（含 question_version/final_reviewer_ids/final_decision）、review_records、audit_logs、batch_jobs（含 owner_id）、ai_review_results、ai_check_tasks、ai_check_discards

级联：删除题目 → 级联删任务/记录/AI 检查结果；删除任务 → 级联删记录。删除题库 → 库内题目自动归未分类（服务层处理）。

## 11. API 摘要

- `/health/live` 进程存活；`/health/ready` 数据库和关键 Schema 就绪（公开、脱敏）
- `/api/auth/*` 登录/注册/me/改昵称/改密
- `/api/permissions` 权限点元数据（登录即可）
- `/api/roles` 角色模板查询（user:manage 或 role:manage）；新增/修改/删除仅 `role:manage`（仅 super_admin）
- `/api/banks` 题库 CRUD（GET 登录即可，管理需 bank:manage）；题库带专业范围 professions，创建/更新时自动归纳
- `/api/banks/{id}/questions` 库内题目（bank:manage）；`POST /api/banks/{id}/collect` 重新归纳
- `/api/meta/professions` 题库中出现的全部专业值（登录即可）
- `/api/users` 用户管理（user:manage，POST/PUT/DELETE；分配角色/权限/题库范围/启用）
- `/api/question-shares` 个人题目分享申请查询与一次性审批（申请：question:share；审批：question:share_review）
- `/api/questions` CRUD（view/create/edit/delete 分权限点）+ generate + search（支持 q/status/bank_id/profession 筛选）+ publish（review:final）
- `/api/questions/{id}/versions` 版本历史；`POST /api/questions/{id}/restore` 恢复历史内容并生成新草稿版本
- `/api/questions/{id}/bank` 手动移入/移出题库（bank:manage）
- `/api/knowledge-points` 列表/搜索（登录即可）/导入增删（knowledge:manage）
- `/api/experts` 列表 + 管理（历史兼容接口，界面已由角色管理/用户管理替代）
- `/api/review/reviewers` 有审题权限的用户列表（流程配置选审核人/名字映射）
- `/api/review/*` submit/action/finalize/task/task-by-question/records/flows + `flows/{id}/revoke`（撤销未完成审核）
- `/api/review/submit-bank` 按题库批量提交审核（user:manage，返回 submitted/skipped_reviewing/skipped_finished/failed）
- `/api/review/results` 审核结果汇总（统计+筛选+专家评语，question:view）
- `/api/review/my-tasks` 待我审核任务列表（仅当前轮分配给我的任务，投完即移开；题目进入下一轮会再次出现）
- `/api/review/my-decisions` 待我决断任务列表（review:final，独立「待决断」页面用）
- `/api/ai-check` POST 检查 + result/results（ai:check）
- `/api/export/xlsx|docx` + download（question:download，下载文件需认证）
- `/api/batch/*` 批量推理（batch:run）
- `/api/audit-logs` 日志（audit:view）
- `/api/stats` 统计（stats:view，含按题库/状态/难度分布）

## 12. DeepSeek 参与以来的修复清单（commit 7404a23 之后）

**状态机**：终态任务禁止再审、AI 检查不回退人工审核状态、rejected 重提更新 FlowID、提交不重置版本号、打回重审主键冲突、编辑已审核题死锁（终态任务+草稿态可建新任务）。

**数据层**：全链路 ON DELETE CASCADE + 幂等迁移、流程配置 5 项校验、删除历史任务流程友好报错、删除流程 nil panic。

**前端**：登录 401 误报"登录已过期"、硬编码 127.0.0.1 → 相对路径+Vite proxy、题库/审核/AI检查页分页加载更多、审核表单权限（分配+已投检查）、重提按钮（rejected/revision_required/草稿态+终态任务）、AI 检查结果版本绑定（"内容已修改"标记）、"未检查"标记、搜索后选择集污染、BankPage button 嵌套非法 HTML、teacher 按钮权限、打回提示框+"去修改题目"直达编辑、解析缺失/过短警告、日志页新动作类型+actor 昵称映射。

**运维**：end.sh 停止 PostgreSQL 卡死（launchctl bootout + pg_ctl 无 -w + pkill 兜底）。

**用户权限体系（2026-08-17）**：用户自主注册、21 个可分配权限点、自定义 n 种角色模板、用户管理权限矩阵勾选 UI、题库分库（内科/外科）与题库范围、审题按库自动匹配 n 位老师、审核投票不再一票否决（全员投票自动统计 → 分歧进入 conflict）、最终把关人决断（流程指定 m 位管理员）、导出下载认证、AI 检查加权限、禁用账号 token 立即失效、统计页新增、权限变更即时生效。

**题库归纳与审核效率（2026-08-18）**：题库按专业搜索、题库管理页指定专业范围自动归纳（创建/重新归纳/新题自动归库）+ 库内手动移入移出微调、删除题库时题目自动归未分类、审核流程配置页一键批量提交题库内所有可提交题目、新增「审核结果」页（状态统计 + 找出问题题目 + 每题专家评语与最终决断）。

**界面简化（2026-08-18）**：移除专家库页面（角色管理/用户管理完全替代，审核人直接从有审题权限的用户中选择）；「多轮审核」更名「单题审核」（审核结果页承担统计总览）；题库管理、审核结果、单题审核页均可点开题目完整详情（题干/选项/答案/解析/大纲代码/知识点/元信息）。

**专家审核工作台（2026-08-18）**：单题审核页新增「待我审核」视图（默认进入）——专家登录直接看到分配给自己的审核任务卡片（流程名/第几轮共几轮/轮次名/题干/投票进度条/通过驳回需修改票数），点击即进入审核；把关人的待审列表同时显示「待决断」任务卡（去决断入口）；批量提交接口返回跳过统计（已在审核中/已审核结束），流程配置页批量提交确认框与题库下拉均提示题库内状态冲突（可提交/审核中/已结束数量）。

**修复（2026-08-18 晚）**：
- 批量提交按钮无反应 bug：`submitFlowBank` 内 `const bankName = bankName(...)` 变量遮蔽导致 TDZ ReferenceError，点击即中断（无弹窗无提示）——已修复并改为失败原因直接提示
- 自动匹配审题人兜底：直接勾选审题权限的用户匹配不到时（如只配置了 expert 角色模板账号），自动兜底使用「角色模板含审题权限且非系统管理员」的用户，避免"没有分配审题人"卡住批量提交
- 流程卡片新增「自动匹配审核人」提示行，批量提交前可预览将由谁审核

**撤销提交（2026-08-18 晚）**：审核流程配置页流程卡片新增「撤销提交」按钮（flow:manage 权限，红色）——管理员可撤回误提交/卡死的流程：删除该流程下所有未完成审核的任务（审核中/需修改/待决断），题目恢复**提交前状态**（任务记录 `question_prev_status` 快照，重提场景也精确恢复）；已审核结束（通过/驳回/入库）的任务与题目不受影响。撤销后专家"待我审核"立即清空，题目可重新提交。`POST /api/review/flows/{id}/revoke`。

**状态机简化（2026-08-25，最终版）**：每轮按「通过票数」门槛推进；最终把关通过后直接进入唯一成功终态 `published`，界面统一显示“已通过”。旧题目/任务状态 `approved` 已迁移，`approved` 仅作为专家投票和决断动作保留。

**待决断独立界面（2026-08-19）**：新增侧边栏菜单「待决断」（review:final 权限专属，把关人主页直接进入）——独立决断工作台：左侧待决断任务卡片（流程名/轮次/题干/票数统计）+ 右侧完整题目信息 + 审核记录 + 决断表单；「待我审核」不再混入待决断任务（一题只要是自己的审核工作结束即移开列表，投完第 1 轮后题目进入第 2 轮会**再次出现在待我审核**）；后端新增 `GET /api/review/my-decisions`。

**投票规则修复（2026-08-19）**：修复"有反对票时任务卡住"的 bug——通过票数规则改为**达到通过票数即过轮**（无视反对票，反对票记录并供最终决断参考），例：通过票数=1 时"1 人驳回+1 人通过"立即推进第 2 轮，两人第 2 轮都能继续看到操作；流程配置新增「投票规则」：**通过票数推进（默认）** / **一票否决（veto，任一驳回直接驳回题目，全员通过才过轮）**，覆盖不同审核流程类型。

**多对多题库 + 搜索优化（2026-08-19 晚）**：
- **一道题可属于多个题库**（question_bank_members 多对多表，自动迁移旧 bank_id 数据）：题库管理页"加入/移出"互不影响其他库归属（加入=POST /questions/{id}/bank，移出=DELETE /questions/{id}/bank/{bankId}，批量=bank-move-batch）；题目详情/列表显示全部所属题库
- **搜索优化**：准确筛选（status/profession/difficulty/outline_code 前缀/bank_id）+ 关键词模糊搜索（题干/答案/ID）；题库管理添加区提供专业/状态/难度/大纲代码精确下拉 + 关键词框组合筛选
- **搜索结果分页**：添加区显示"共 N 道，已加载 M 道"+「加载更多」+「全选全部结果」（自动加载剩余页全选）；批量加入走高效 SQL（410 道一次完成），不再逐题循环
- 移除「重新归纳」按钮（自动归纳在创建/更新题库时执行，手动添加走搜索勾选）
- 审核自动匹配/权限过滤/提交过滤均按多对多归属判断（题目属于任一可见库即可见/可提交）
- 单题审核页精简为纯「待我审核」工作区（删除「全部题目」视图，浏览/管理题目全部由题库页承担）
- **提交审核权限收归管理员**：`review:submit` 权限点移除（21→20 个），提交审核/批量提交接口仅 `user:manage` 可调用；题库管理页可搜索勾选单个/批量添加题目（含审核中/已通过等状态）；审题人/出题人无法提交审核（403），只负责投票
- 待决断任务不在「待我审核」出现（独立「待决断」页面），revision_required 任务暂不显示（管理员编辑重提后恢复）
- 创建题库成功后自动展开详情，可直接搜索/批量添加题目

**知识点导入去重 + 自主添加（2026-08-20）**：
- **导入去重**：同一大纲代码且内容完全一致 → 跳过（不重复导入）；内容有变化 → 更新；导入接口返回「新增/更新/跳过重复」统计，前端导入提示显示明细（实测重复导入 7235 条全部跳过）
- **自主添加知识点**：知识点页新增「+ 添加知识点」表单（大纲代码/分类/专业/单元/细目/要点）；填了大纲代码则以其为主键，重复添加自动更新不产生重复
- **出题 prompt 已使用知识点完整详情**：单题（generator.BuildPrompt）与批量 jsonl（batch.generateJSONL 复用同一 prompt）均包含大纲代码/分类/专业/单元/细目/要点
- **注册产品口径（2026-09-02）**：自助注册默认开启，关闭时入口隐藏且接口返回403；注册用户为空角色、零可分配业务权限、空题库范围，只能登录和访问基础页面。管理员端用户列表自动出现“新注册”账号，可分配角色、直接权限、题库范围和启用状态
- **注册/登录防滥用（2026-09-02）**：登录账号桶第8次失败、来源IP桶第30次失败开始短时指数限速；同IP注册前5次允许、第6次开始限速。失败审计仅存账号/IP短哈希，不记录密码、Token或完整IP；不存在账号执行dummy bcrypt，客户端不区分用户名不存在、密码错误或账号禁用

**知识点选择器（2026-08-20）**：新增共享组件 `KnowledgePointPicker.vue`，AI出题页与批量出题页统一使用：
- **模糊搜索**（关键词匹配 topic/单元/细目/专业/大纲代码）+ **精确筛选**（分类下拉、专业下拉、大纲代码前缀），组合使用
- 输入防抖 300ms（修复原 AI 出题页搜索卡顿）+ 结果分页（页码翻页）
- AI出题页：单选知识点；批量出题页：**勾选多选**（跨页保留）+ 逗号分隔大纲代码批量加入 + 已选标签移除
- 批量出题配置简化：移除「生成数量（知识点数）」与「起始/结束大纲代码」输入栏，保留「每知识点题数」「跳过已有题目」「任务名称」；提交按钮显示"N 个知识点 × M 题"
- 后端：知识点搜索接口支持精确筛选+分页（`/api/knowledge-points/search` 参数 q/subject/category/outline_code/page/page_size），新增 `/api/knowledge-points/meta`（分类/专业列表）

**服务端全量测试修复（2026-09-10）**：Word（docx）导出改为标准库直接生成 OOXML 包，去掉 pandoc 外部依赖，容器部署不再 500（含 OOXML 结构与 XML 转义单测）；送审状态错误补充 `ErrReviewNotSubmittable`（409）和 `ErrReviewBadRequest`（400）哨兵错误，驳回终态重提不再返回 500；新增 review 层哨兵错误回归测试。

**界面收敛与权限收归（2026-08-19）**：
- 单题审核页精简为纯「待我审核」工作区（删除「全部题目」视图，浏览/管理题目全部由题库页承担）
- **提交审核权限收归管理员**：`review:submit` 权限点移除（21→20 个），提交审核/批量提交接口仅 `user:manage` 可调用；**所有审核提交只在自创建题库范围进行**（审核流程配置页「批量提交审核」），大题库浏览页（题库）无任何提交审核入口；审题人/出题人无法提交审核（403），只负责投票
- 待决断任务不在「待我审核」出现（独立「待决断」页面），revision_required 任务暂不显示（管理员编辑重提后恢复）

## 13. 遗留问题（未修复，按优先级）

1. **pqArray 旧数据检查**：store.go 已实现转义，但 keywords/specialties 等历史数据仍应做一次专项扫描
2. **题型边界收紧**：系统只处理纯文本 A2 单选题，不再保留题目附图链路
3. **小项**：知识点导入列格式错误提示不友好、BatchPage 并发轮询、审计日志历史数据
4. **账号后续企业能力**：当前自助注册+管理员授权+限速审计已完成；密码找回、MFA、OIDC/SAML、注册通知和集中告警按首批客户身份要求再做

> 已修复（本次权限体系改造）：导出下载接口无认证、AI 检查接口无权限限制、JWT 不校验用户启用状态。

## 14. 环境与启动

```bash
# 一键启动（PostgreSQL + 后端8080 + 前端5173）
./start.sh
# 一键停止
./end.sh

# 环境变量（.env，不提交）
DB_DSN / JWT_SECRET（必填，serve 强制校验非默认值）；QWEN_BASE_URL / QWEN_MODEL / QWEN_API_KEY 可用于首次兼容导入，实时配置上线后由超级管理员在“AI 服务配置”维护
AIGO_REGISTER_ENABLED（注册开关；默认1，设0/false可紧急关闭）/ AIGO_TRUST_PROXY_HEADERS（仅受控反向代理部署开启）/ AIGO_ADMIN_PASSWORD（空库首次创建admin的密码，留空=随机生成并打印）
CORS_ALLOWED_ORIGINS（跨域白名单，逗号分隔，空=禁止跨域）
AIGO_HTTP_ADDR（正式监听地址）/ AIGO_WEB_DIST_DIR（前端生产构建目录）/ AIGO_ENV_FILE（外部 dotenv；-=禁用）
AIGO_OFFSITE_*（可选异机备份：age接收者公钥、S3端点/bucket/prefix、扫描/超时和Object Lock确认；真实凭证只放Compose Secrets）

# 默认账号
 admin / admin          # 仅演示库；新库首次启动时创建为唯一 super_admin，密码随机生成或由 AIGO_ADMIN_PASSWORD 指定（空库才创建）
expert / expert        # 医学专家（内置 expert 角色）
expert2 / expert2      # 张主任（内置 expert 角色）
teacher / teacher      # 命题教师（内置 teacher 角色）
# 当前产品要求默认开放注册；注册后无业务权限，由管理员在「用户管理」分配角色、权限和题库范围

# 测试命令
go build ./... && go vet ./... && go test ./...
```

**注意**：本机 `brew services` 有 bug（`stop_timeout` 方法缺失），启停 PostgreSQL 不要用 brew services，用 start.sh/end.sh。

`start.sh` 会先构建后端并独立执行 `aigo migrate`，迁移日志写入 `output/migrate.log`；随后通过公开健康接口确认 8080 后端真正就绪。数据库迁移或端口启动失败时会显示对应日志并退出，不会继续启动前端或虚报成功。

正式模式执行 `./build-production.sh` 后再运行 `./run-production.sh`。正式脚本不启动 PostgreSQL、不后台驻留，也不读取项目 `.env`；应由 systemd、launchd、Docker 等外部进程管理器提供环境/Secrets、守护、重启和日志收集。每次构建保留不可变 release，运行中进程解析到物理目录，因此切换 `output/production` 不会让旧进程混用新静态资源。

容器交付按 `deploy/README.md` 操作：复制生产环境模板、生成 Secret、检查 Compose 后启动。Compose 只公开 Caddy 80/443；数据库 5432 和应用 8080 不映射宿主机。本地 Qwen 是外部服务，必须填写容器可访问的内网地址。

备份服务默认启动后立即执行一次，之后每日运行，本地保留 14 天且最多 30 份。备份卷与 PostgreSQL 卷分离，但仍位于同一 Docker 主机；正式环境应按 `deploy/README.md` 启用独立 `offsite` 服务，在服务器侧用 age 公钥加密后自动上传对象存储。`drill` 只恢复到临时库并在指纹校验后删除，`restore` 只允许新的数据库名。

异机服务默认不启用，且从主 Compose 中分离，缺少 S3 配置不会影响现有应用、本地备份或恢复。应用服务器只保留 age 接收者公钥；私钥必须离线保存。客户端不可覆盖上传不能单独防删除，客户 bucket 必须开启版本控制和默认 Object Lock/WORM，并使用无删除、无绕过保留权限的上传凭证；本地模拟 S3 未验证真实 WORM，上线时必须单独验收。

## 15. 测试方法备忘（已建立的实践）

- **后端 API**：curl + jq，token 从 `/api/auth/login` 获取存 /tmp
- **健康探针**：`curl -f /health/live` 与 `curl -f /health/ready`；ready=503 时查看服务端日志，响应体不会回显底层数据库错误
- **数据库迁移**：`go run ./cmd/aigo migrate`；重复执行应报告“已是最新版本”。设置 `AIGO_TEST_POSTGRES_DSN` 后，`go test -race ./internal/storage/postgres` 会覆盖空库并发迁移、幂等、失败回滚、旧表兼容和迁移记录篡改
- **正式构建**：`./build-production.sh`；验证 `output/production/aigo`、`output/production/web/index.html` 和 `output/production/web/favicon.svg`。重复构建应切换到新物理 release，旧 release 保持不变
- **正式浏览器回归**：在外部 PostgreSQL 已启动且生产环境变量已注入时执行 `./run-production.sh`，验证 `/health/ready`、`/login`、题库和审核结果深层链接直接访问/刷新；`/.env` 与缺失 `/assets/*` 必须为 404
- **容器部署回归**：隔离项目名执行 `docker compose ... up -d --build`；检查迁移容器 exit=0、应用/数据库 healthy、错误 Host=421、HTTP→HTTPS=308、未认证 API=401、5432/8080 未发布、Secret 不在容器环境。再执行数据库停机恢复、应用进程 SIGKILL、`down/up` 不删卷和持久化数据复查
- **备份恢复回归**：缩短 UAT 备份间隔并限制最多 2 份，验证自动清理和 health；执行 `create/verify/drill/restore/export`，修改 archive 一个字节必须校验失败，恢复核心表指纹必须一致，当前业务数据库名必须被拒绝，容器 down/up 后备份卷仍存在
- **异机备份回归**：验证三件套源校验、age密文不含明文、不可覆盖上传、receipt最后提交和远端完整回读SHA；重复扫描/状态丢失幂等，同名冲突、远端篡改、缺失/空下载拒绝，S3断网后重试/重启恢复，跨容器锁串行。最终必须用真实 PostgreSQL archive 完成“上传→下载→离线解密→import→verify→drill”指纹闭环；客户实际 Object Lock 另做存储端覆盖/删除拒绝验收
- **注册授权回归**：注册后断言 role/permissions/direct_permissions/bank_ids均为空，未授权访问管理接口为403；管理员列表必须自动出现“新注册”，赋权后既有Token后端权限立即生效，重新登录后前端菜单同步
- **登录限速回归**：账号前7次失败为通用401、第8次起429；同IP不同账号第30次失败触发IP桶；同IP注册前5次允许、第6次429。审计详情不得出现完整用户名、IP、密码或Token
- **前端 UI**：Playwright（`/Users/imaichika/anaconda3/bin/python3`，`channel='chrome'`）
  - 注意：点击带 `confirm()` 的按钮必须 `page.on("dialog", lambda d: d.accept())`，否则看似"按钮无反应"
  - 页面跳转后等待 `networkidle` + 状态文本断言
- **数据库直查**：`export $(grep -v '^#' .env | xargs); psql "$DB_DSN" -c "..."`
- **状态收口回归**：题目/任务表禁止写入生命周期 `approved`；迁移后成功终态统一为 `published`，审核记录中的 `approved` 通过票保持不变
- **审核事务回归**：`go test ./internal/review/` 覆盖投票、决断、重提、撤销和六类故障回滚；设置 `AIGO_TEST_POSTGRES_DSN` 后额外执行真实 PostgreSQL 跨表回滚与双连接池并发测试
- **UI 全流程脚本**：`/tmp/aigo_ui_test.py`（注册→权限矩阵分配→建题库→提交审核→投票冲突→把关决断，29 项断言）

## 16. 下一步建议（按价值排序）

1. **先完成实际存储上线验收**：在首个客户 S3/MinIO 上验证版本控制、默认 Object Lock/WORM、无删除/无绕过权限、保留期和失败通知；这是部署验收，不再新增另一套备份代码
2. **镜像发布与供应链**：私有镜像仓库、固定版本标签、SBOM、漏洞扫描、签名/验签、release 清理及应用+数据库回滚边界
3. **补齐可观测与真实多用户能力**：结构化应用日志、监控指标和告警；先把数据库、应用、备份和异机上传失败送达值班人员，再补用户配额、任务归属、审题大待办分页/筛选和数据库查询下推
4. **建设最小持久任务层**：将文本出题、AI 检查和批量导入改成可持久化任务，提供幂等、取消、有限重试、并发/背压和失败恢复
5. **完成本地 Qwen 生产验证后再增加受控 workflow**

> 已修复：JWT默认密钥拒启、默认admin仅空库创建（随机密码）、题目/审核接口题库范围校验、CORS白名单、请求体10MB上限、HTTP超时与优雅关闭；2026-09-02按产品要求统一为“注册默认开启、注册后最低权限、管理员授权”，并加入注册/登录限速和脱敏审计；同日完成客户端加密异机备份及真实数据库恢复链路，本地模拟环境不替代客户存储的WORM验收。

## 17. 给下一个 AI 对话的快速上手路径

1. 读本文件 + `功能需求文档.txt` + `CLAUDE.md`（5 分钟）
2. `./start.sh` 启动，浏览器开 http://localhost:5173 用 admin/admin 逛一遍 15 个页面
3. 在审核页点 flow-2experts 的 5 道题理解状态机；「用户管理」页看权限矩阵
4. 改代码：先 `go build ./... && go vet ./...`；前端改完 Vite 自动热更新
5. 每个改动跑一次相关流程回归（curl 或 Playwright 参考第 15 节）
6. commit 规范：中文描述 + `Co-Authored-By: DeepSeek <deepseek@ai>`

## 18. 推理部署切换基础（2026-08-24）

- 实时文本链路保留统一 `llm.Client`，新增 `QWEN_DEPLOYMENT=cloud|local`。云端继续调用百炼；本地可接 vLLM/SGLang 等 OpenAI-compatible `/v1/chat/completions`，允许无 API Key 或使用独立网关 Key。
- 云端 `qwen3.5-flash` 与本地开放权重的模型 ID 不相同。本地对应权重应使用实际 served-model-name（官方对应模型为 `Qwen/Qwen3.5-35B-A3B`），不得在业务代码中把两者写成同一个固定值。
- 思考模式已在客户端适配：百炼使用顶层 `enable_thinking`；本地 Qwen 服务使用 `chat_template_kwargs.enable_thinking`。
- 实时推理与百炼 Batch 凭证已经解耦。本地实时模式不会自动携带 `DASHSCOPE_API_KEY`，`QWEN_BATCH_BACKEND=auto` 也默认不把题目外发；渐进迁移期若仍需百炼 Batch，必须显式设置 `QWEN_BATCH_BACKEND=dashscope`。
- 批量层新增 `batch.Executor`。现有 DashScope Files + Batches 功能保留，模型名不再硬编码，Prompt 复用 `generator` 的统一实现；`local` 批量值目前是明确的不可用占位，不能假装本地实时端点支持 `/files`、`/batches`。
- 批量 API 增加 provider 路由：历史无前缀任务 ID 继续按 DashScope 处理，未来非云后端使用 `backend:provider_job_id`，切换默认后端后不会把旧云任务误发给本地 adapter。`batch_jobs` 已固化 backend、脱敏 profile 和提交时模型；旧任务模型未知时保持为空，不用当前模型冒充历史模型。
- 批量页通过 `/api/batch/capabilities` 展示脱敏能力和模型，后端不可用时返回明确状态并禁止提交。
- 实时部署地址、API Key、生成模型和 AI 检查模型由唯一超级管理员在 Web “AI 服务配置”页面维护；API Key 使用 JWT_SECRET 派生密钥加密落库，接口只返回脱敏值。模型服务进程、模型路径、量化和 GPU 参数仍由部署运维管理；Batch 配置继续独立保留。
- 新增 `internal/llm`、`internal/config`、`internal/batch` 回归测试；验证命令仍为 `go test ./...`，前端为 `cd web && npm run build`。

仍未实现：本地持久批量任务队列、取消/重试/重启恢复、幂等导入、完整生成运行记录（prompt/token/耗时）、本地模型健康检查，以及同一 provider 多 profile 的历史路由 registry。这些是后续工业化任务，不应在当前界面宣称已完成。

## 19. 当前路线决策（2026-08-28）

### 19.1 已确认的产品与部署方向

- **题目形态**：当前只支持纯文本 A2 单选题。
- **文本推理最终走本地部署**：目标是在未来真实环境使用本地部署的 Qwen 3.5 模型服务；当前只完成了 OpenAI-compatible 接口和配置切换预留，尚未完成本地推理基础设施与生产验收。
- **最终面向真实多用户环境**：当前 `start.sh + Homebrew PostgreSQL + Vite dev server` 仅限本地开发，未来需支持多用户并发、长期运行、服务重启、权限隔离、任务恢复、监控、备份和升级。
- **近期按单机构多用户设计**：优先采用“一个客户/机构一套部署，机构内多个管理员、命题教师和审核专家”的交付形态。多个机构共享同一数据库的多租户 SaaS 需要额外的 tenant 数据隔离、租户级密钥/配额/审计和迁移策略，除非后续产品需求明确提出，否则当前不提前实现。
- **AI 仍是辅助命题工具**：AI 生成、AI 复核和 AI 修改都只能产生草稿或建议；任何 workflow/Agent 都不得绕过人工审核或直接发布正式题目。

### 19.2 Workflow 与生产部署的先后关系

当前不先引入 LangGraph、n8n、Dify、Coze 或自由自治 Multi-Agent。应按以下顺序推进：

```text
生产部署目标与运行边界
        ↓
正式部署基础 + 多用户安全/可观测
        ↓
持久任务层（重试、幂等、恢复、并发、运行记录）
        ↓
本地 Qwen 3.5 接入与质量/容量验收
        ↓
受控的生成—复核—修改 workflow
        ↓
小规模真实用户试点与指标回收
        ↓
```

原因：当前真正的阻断项是同步外部调用、开发态启动方式、缺少任务恢复和生产观测。直接先做 Agent workflow，会把多个不可靠的同步调用串成一个更长、更难恢复的调用链。近期需要实现的是 workflow 的**可靠运行底座**，而不是复杂的 Agent 编排框架。

### 19.3 引入受控 Workflow 的前置条件

只有满足以下条件后，才开始实现自动“生成—复核—修改”循环：

- 单次文本生成和 AI 检查已经任务化，进程重启后可继续或安全重试。
- 每次调用都有 run/step/attempt、题目版本、模型、prompt 版本、耗时、token 和失败原因记录。
- 本地模型已经完成固定医学 A2 题基准集评测和基本容量压测。
- 循环有确定性路由和硬上限，默认最多修改 1-2 次，失败后转人工或重新生成。
- 正式发布仍由现有审核事务、题目版本校验和人工最终把关完成。
