# AIgo 项目进展指引（2026-08-15 更新）

> 本文件是项目当前状态的完整快照，供新的 AI 对话/开发者快速上手。
> 产品基准永远是根目录《功能需求文档.txt》和《CLAUDE.md》，冲突时以它们为准。

## 1. 项目一句话定位

面向国家执业医师考试 **A2 型试题**的 AI 辅助命题系统：
知识点 Excel / 大纲 → 千问出题（草稿）→ AI 质量检查（可选）→ 多轮人工审核（可配置）→ 管理员发布 → 正式题库，支持 xlsx/docx 导出和 AI 配图。

## 2. 当前状态速览

- **Git**：分支 `main`，最新 commit `7ab5340`（领先 origin/main 4 个 commit，未 push）
- **代码健康**：`go build ./...`、`go vet ./...`、`go test ./...` 全过
- **服务状态**：当前全部停止（可用 `./start.sh` 一键启动）
- **数据库**：PostgreSQL@16（brew 安装），13 张表，数据目录 `/opt/homebrew/var/postgresql@16`
- **题库数据**：约 5260 道题（5258 道 auto_checked 批量生成 + 1 道 ai_draft + 1 道 ai_reviewed），知识点 7235 个
- **审核数据（测试痕迹，保留）**：流程 `flow-2experts`（双专家两轮）+ 5 道题四结局示例（1 published / 2 approved / 1 rejected / 1 revision_required），可直接在审核页/日志页查看

## 3. 必读文件

| 文件 | 作用 |
|---|---|
| `功能需求文档.txt` | 产品需求基准（树状图） |
| `CLAUDE.md` | 开发约束（Go 为主、AI 结果默认草稿、不做知识图谱等） |
| `项目结构.txt` | 目录结构和阅读顺序 |
| `当前还缺的功能提示.txt` | 待开发清单 |
| `问题.txt`/`problem.txt` | 历史 bug 清单（已大部分修复） |
| **本文件** | 项目进展指引 |

## 4. 技术栈

- **后端**：Go 1.22+（net/http 标准库路由 `method /path/{param}`），仅依赖 `lib/pq`、`excelize`
- **前端**：Vue 3 + Vite 7 + vue-router（Element Plus 未实际使用），端口 5173，proxy `/api` 和 `/images` → 8080
- **数据库**：PostgreSQL 16，启动时自动建表 + 幂等迁移（`store.go runMigrations`）
- **LLM**：千问 OpenAI 兼容接口（`DASHSCOPE_API_KEY`/`QWEN_BASE_URL`/`QWEN_MODEL`，默认 qwen3.5-flash）
- **生图**：通义万相 qwen-image-2.0（`internal/image/zimage.go`，提示词由千问生成，3-5 张候选图）
- **批量出题**：DashScope 批量 API（云端 JSONL 任务，`batch-run/batch-status/batch-download`）
- **测试工具**：后端 go test；前端 Playwright（anaconda python3，系统 Chrome 通道）

## 5. 目录结构（当前）

```
cmd/aigo/main.go        CLI 入口（serve/import/kp-import/batch-run/export 等 30+ 命令）
internal/
  api/                  HTTP handler（按资源拆分，server.go 注册路由）
  auth/                 JWT + RBAC（admin/expert/teacher 权限表在 auth.go CheckPermission）
  aicheck/              AI 质量检查（四维评分 + 问题 + 建议，结果带 question_version）
  audit/                审计日志（submit/resubmit/review/publish/flow_*/expert_*/user_* 等动作）
  batch/                DashScope 批量推理
  config/               .env + 环境变量加载（JWT_SECRET 也从 env 读）
  domain/               领域模型（题目/知识点/审核/AI检查类型 + 状态常量）
  evaluator/            规则评估器（基本未在 Web 流程启用）
  exporter/             docx 导出（pandoc 方案）
  generator/            出题 prompt + JSON 解析（含 SanitizeIDPrefix）
  image/                图片服务（提示词生成/生图/图片审核）+ zimage 生图器
  importer/             xlsx 导入（题目/知识点大纲）
  knowledge/            知识点服务
  llm/                  千问客户端
  multimodal/           多模态接口 stub（预留）
  pipeline/             编排层（CLI 用）
  review/               审核服务（核心：SubmitQuestion/Review/CreateFlow 等）
  storage/interfaces.go 存储接口定义
  storage/postgres/     PG 实现（schema.sql + store.go + 幂等迁移）
  storage/testutil/      内存实现（测试用）
web/src/
  api.js                API 封装（BASE=/api 相对路径，401 跳过登录接口）
  auth.js               token/用户状态（isAdmin/isLoggedIn/currentUser）
  router.js             路由 + 守卫（admin 页面重定向）
  App.vue               侧边栏（admin 菜单按角色显示）
  pages/                11 个页面（Login/Generate/Knowledge/AICheck/Review/Bank/Batch/Experts/ReviewFlow/Audit/Users）
configs/review_flows.json  审核流程初始配置（仅空库时导入）
start.sh / end.sh       一键启停（end.sh 已修复 launchd KeepAlive 卡死问题）
```

## 6. 核心业务流程（题目状态机）

```
ai_draft（草稿，出题/批量导入默认）
  │  AI 检查（可选）verdict=pass 且状态为草稿态
  ▼
ai_reviewed ──────────┐
  │                   │（auto_checked：历史遗留状态，规则评估器未启用）
  │ 提交审核 SubmitQuestion（白名单：ai_draft/auto_checked/ai_reviewed/revision_required/rejected）
  ▼
reviewing（任务按流程轮次推进，required_count 门槛，admin 可审任意轮）
  ├─ 专家 approved 达门槛 → 进下一轮 / 全部轮次完成 → approved
  ├─ 专家 revision_required → 任务+题目 revision_required（本轮票清零，保持当前轮）
  │     → 编辑题目（版本+1）→ 重新提交 → reviewing（回到被打回的轮）
  └─ 任一专家 rejected → 任务+题目 rejected（终态，一票否决）
        → 编辑题目 → 重新提交 → 任务重置回第 1 轮从头审核
approved → 管理员发布 → published（正式题库）
已 approved/published 题目被编辑 → 自动回退 ai_draft（旧任务终态时重新提交会创建新任务）
```

**关键规则**：
- AI 检查**只能推进草稿态**，绝不能回退 reviewing/approved/published（人工审核优先）
- 终态任务（approved/rejected）禁止再审核（Review 里有状态校验）
- 审核记录 ID 带时间戳，同一专家打回后可重审
- 提交审核写 submit/resubmit 日志，审核写 review 日志（含意见），全程留痕

## 7. 审核流程配置（可投入使用）

- 前端 `/review-flows`（仅 admin）：新建/编辑/删除流程
- 后端校验（`review.CreateFlow/UpdateFlow`）：专家存在且启用、同轮去重、`1 ≤ required_count ≤ 专家数`、重复流程 ID 拒绝
- **编辑保护**：有进行中任务（reviewing/revision_required）的流程禁止修改/删除；有历史任务的流程禁止删除（保留追溯）
- 专家账号关联：专家库直建的专家（E 前缀）**无登录账号**，流程配置页有红色"无账号"标记——流程里必须选有账号的专家（用户管理创建的 expert 角色账号自动同步专家库，ID 一致）
- 配置的 `can_modify`/`is_required`/`subject` 字段当前**未在后端使用**（死配置）

## 8. 权限矩阵（RBAC）

| 能力 | admin | expert | teacher |
|---|---|---|---|
| 出题/列表/查看 | ✅ | ✅ | ✅ |
| 删除题目 | ✅ | ✅ | ❌ |
| 提交审核 | ✅ | ✅ | ✅ |
| 执行审核 | ✅（任意轮） | ✅（仅分配轮） | ❌ |
| 发布题目 | ✅ | ❌ | ❌ |
| 知识点查看/搜索 | ✅ | ✅ | ✅ |
| 知识点导入/增删 | ✅ | ✅ | ❌ |
| 专家库查看 | ✅ | ✅ | ❌ |
| 专家库增删改/流程配置/日志/用户管理 | ✅ | ❌ | ❌ |
| AI 检查 | ✅ | ✅ | ✅（仅登录即可） |
| 配图生成/审核 | ✅ | ✅ | ❌ |

前端已同步：admin 菜单按角色显示、路由守卫重定向、删除/导入按钮按角色隐藏、发布按钮仅 admin。

## 9. 数据库表（13 张）

users、knowledge_points、questions、experts、review_flows、review_tasks、review_records、audit_logs、image_prompts、generated_images、image_review_records、batch_jobs、ai_review_results（含 question_version）

级联：删除题目 → 级联删任务/记录/图片/AI 检查结果；删除任务 → 级联删记录；删除流程配置 → 级联删图片。

## 10. API 摘要

- `/api/auth/*` 登录/me/改昵称/改密
- `/api/users` 用户管理（admin，创建时校验：用户名唯一、密码≥8位、角色合法；expert 角色自动同步专家库）
- `/api/questions` CRUD + generate + search + publish + 分页（page/page_size 默认100上限500）
- `/api/knowledge-points` 列表/搜索/导入（multipart，列：序号、分类、专业/系统、单元、细目、要点、大纲代码）
- `/api/experts` CRUD（直建专家 ID 为 E 前缀，无账号）
- `/api/review/*` submit/action/task/task-by-question/records/flows（POST/PUT/DELETE）
- `/api/ai-check` POST 检查 + result/results
- `/api/images/*` 提示词/生成/列表/审核
- `/api/export/xlsx|docx` + download
- `/api/batch/*` 批量推理（提交/状态/下载/列表）
- `/api/audit-logs` 日志（整体/按题目/按操作人）
- `/api/stats` 统计（公开）

## 11. DeepSeek 参与以来的修复清单（commit 7404a23 之后）

**状态机**：终态任务禁止再审、AI 检查不回退人工审核状态、rejected 重提更新 FlowID、提交不重置版本号、打回重审主键冲突、编辑已审核题死锁（终态任务+草稿态可建新任务）。

**数据层**：全链路 ON DELETE CASCADE + 幂等迁移、流程配置 5 项校验、删除历史任务流程友好报错、删除流程 nil panic。

**前端**：登录 401 误报"登录已过期"、硬编码 127.0.0.1 → 相对路径+Vite proxy、题库/审核/AI检查页分页加载更多、审核表单权限（分配+已投检查）、重提按钮（rejected/revision_required/草稿态+终态任务）、AI 检查结果版本绑定（"内容已修改"标记）、"未检查"标记、搜索后选择集污染、BankPage button 嵌套非法 HTML、teacher 按钮权限、打回提示框+"去修改题目"直达编辑、解析缺失/过短警告、日志页新动作类型+actor 昵称映射。

**运维**：end.sh 停止 PostgreSQL 卡死（launchctl bootout + pg_ctl 无 -w + pkill 兜底）。

## 12. 遗留问题（未修复，按优先级）

1. **并发审核竞态**：Review() 读-改-写无锁，两专家同时审核会丢票（建议行锁/事务）
2. **并发双提交**：同一题并发 submit 可产生两个 reviewing 任务（建议 question_id 唯一约束或锁）
3. **审核中题目可被静默编辑**：reviewing/revision_required 状态被编辑不改状态不通知审核人（`RoundConfig.CanModify` 未启用，建议启用该字段）
4. **pqArray 无转义**：keywords/specialties 含逗号或引号会坏（换 `pq.Array()`）
5. **图片审核前端无入口**：后端 API 已有，前端无通过/驳回按钮（用户暂时搁置）
6. **AI 检查接口无权限限制**（任何登录用户可消耗额度）、**导出下载接口无认证**
7. **JWT 不校验用户启用状态**（停用后 token 24h 内仍有效）
8. **版本快照缺失**：`question_versions` 表已移除，修改内容无历史快照（需求要求留痕，当前只有 version 数字）
9. **小项**：知识点导入列格式错误提示不友好、审核页 teacher 无 review:action 却可看审核页（表单已隐藏）、BatchPage 并发轮询、审计日志 41 条历史数据

## 13. 环境与启动

```bash
# 一键启动（PostgreSQL + 后端8080 + 前端5173）
./start.sh
# 一键停止
./end.sh

# 环境变量（.env，不提交）
DASHSCOPE_API_KEY / QWEN_BASE_URL / QWEN_MODEL / DB_DSN / JWT_SECRET

# 默认账号
admin / admin          # 管理员
expert / expert        # 医学专家（专家库）
expert2 / expert2      # 张主任（专家库）
teacher / teacher      # 命题教师

# 测试命令
go build ./... && go vet ./... && go test ./...
```

**注意**：本机 `brew services` 有 bug（`stop_timeout` 方法缺失），启停 PostgreSQL 不要用 brew services，用 start.sh/end.sh。

## 14. 测试方法备忘（已建立的实践）

- **后端 API**：curl + jq，token 从 `/api/auth/login` 获取存 /tmp
- **前端 UI**：Playwright（`/Users/imaichika/anaconda3/bin/python3`，`channel='chrome'`）
  - 注意：点击带 `confirm()` 的按钮必须 `page.on("dialog", lambda d: d.accept())`，否则看似"按钮无反应"
  - 页面跳转后等待 `networkidle` + 状态文本断言
- **数据库直查**：`export $(grep -v '^#' .env | xargs); psql "$DB_DSN" -c "..."`
- **测试痕迹**：审核流程 `flow-2experts` 与 5 道四结局例题保留在库中，可反复用于演示/回归

## 15. 下一步建议（按价值排序）

1. 修并发审核竞态（P1）——多人真实使用时会丢票
2. 启用 `CanModify`：配置允许修改的轮次，审核页专家可直接改题留痕
3. 补题目版本快照表 + 修改历史查看页
4. 图片审核前端页面（后端已就绪）
5. 知识点版本管理（需求 02，用户搁置中）
6. 生产部署：JWT_SECRET 换强密钥、导出下载加认证、AI 检查加权限

## 16. 给下一个 AI 对话的快速上手路径

1. 读本文件 + `功能需求文档.txt` + `CLAUDE.md`（5 分钟）
2. `./start.sh` 启动，浏览器开 http://localhost:5173 用 admin/admin 逛一遍 11 个页面
3. 在审核页点 flow-2experts 的 5 道题（四种状态都有）理解状态机
4. 改代码：先 `go build ./... && go vet ./...`；前端改完 Vite 自动热更新
5. 每个改动跑一次相关流程回归（curl 或 Playwright 参考第 14 节）
6. commit 规范：中文描述 + `Co-Authored-By: DeepSeek <deepseek@ai>`
