# AIgo 工业化使用差距分析

## 一、总体结论

当前项目已经达到“功能演示/内部试用”阶段，但还不具备真实客户工业化使用条件。按工业产品标准估计，当前成熟度约为 4/10。

核心差距集中在：

- 安全边界与权限隔离
- 审核可信性与发布安全
- 数据版本和审计追溯
- 并发一致性与任务可靠性
- AI 成本、质量和结果治理
- 生产部署、监控、备份和恢复

当前项目适合继续进行内部试用和流程验证，不建议直接部署给真实客户，更不建议让其承载正式考试题库或未经人工复核的医学内容。

## 二、当前验证结果

- `go test ./...` 通过
- `go vet ./...` 通过
- `go build ./...` 通过
- 前端 `npm run build` 通过
- 未发现 API、端到端、并发、安全、迁移回滚类测试

## 三、上线前必须解决的阻断问题

### P0：安全和权限

1. 默认管理员账号为 `admin/admin`，JWT 存在默认密钥。生产环境可能被直接接管。
   - 证据：`cmd/aigo/main.go`、`internal/config/config.go`

2. 题库范围权限只用于列表过滤，详情、修改、删除、发布、图片和审核接口未统一校验题库范围。
   - 影响：掌握题目 ID 的用户可能操作无权访问的题目。
   - 证据：`internal/api/handler_question.go`

3. 图片审核接口直接接受请求中的 `expert_id`。
   - 影响：可伪造其他专家身份提交图片审核意见。
   - 证据：`internal/api/handler_image.go`

4. CORS 当前允许所有来源，图片目录直接作为公开静态目录提供。
   - 证据：`internal/api/handler_utils.go`、`internal/api/server.go`

5. JWT 放在浏览器 `localStorage`，没有刷新、撤销、设备会话、MFA、登录失败锁定或完善的密码策略。

6. 用户可以公开注册，需要根据客户环境决定是否改为邀请制、审批制或接入企业 SSO。

### P0：审核和发布可信性

1. 发布只检查题目状态，不核对完整审核任务、审核版本和图片审核状态。
   - 影响：可能绕过审核或发布未通过审核的带图题。
   - 证据：`internal/review/service.go` 的 `PublishQuestion`

2. 题目只有递增版本号，没有版本快照、差异和恢复机制。
   - 影响：无法证明专家审核时看到的内容，也无法追溯谁改了什么。
   - 证据：`internal/storage/postgres/schema.sql` 的 `questions` 表

3. 审核投票采用“读完整任务—修改 JSON—整体写回”，没有行锁或乐观锁。
   - 影响：两名专家同时审核时可能互相覆盖投票结果。
   - 证据：`internal/review/service.go` 的 `Review`

4. 题目状态更新、审核记录、审核任务不是一个事务。
   - 影响：可能出现题目已进入审核但任务未创建、记录已写入但状态更新失败等不一致。

5. 文档描述为“审核通过后管理员发布”，但最终决断通过时服务会直接将题目设置为 `published`。
   - 影响：发布流程和实际业务规则不一致，需要明确唯一的发布闸门。

### P0：请求和文件处理

1. HTTP 请求体和文件上传使用无上限 `io.ReadAll`。
   - 影响：大请求或恶意上传可能耗尽服务内存。
   - 证据：`internal/api/handler_utils.go`、`internal/api/helper.go`

2. HTTP 服务没有读写超时、空闲超时和优雅关闭。
   - 证据：`cmd/aigo/main.go` 使用 `http.ListenAndServe`

## 四、AI 和业务流程方面的差距

1. 基础校验不够严格。生成失败的题目仍会保存为草稿，只把错误写入 `SourceRefs`；人工新建和编辑也没有统一执行 4～5 个选项、选项非空、答案唯一等校验。

2. 规则评估器没有真正接入 Web 主流程。生成流程主要执行基础 `Validate`，自动质量评估仍偏向 CLI 操作。

3. 没有完整记录每次生成的 prompt、模型版本、耗时、token、成本和原始生成上下文，难以审计和复盘。

4. 图片审核通过后没有完整的题目素材绑定闭环；图片状态和题目正式发布状态之间没有强约束。

5. 批量结果没有明确的“已导入”幂等标记，重复点击下载可能重复导入。

6. AI 生成和生图没有用户级限额、并发限制、成本预算、任务队列和取消机制。

7. 审核流程中“驳回”“退回修改”“重新提交”的语义仍需统一，避免同一状态存在多条相互冲突的操作路径。

## 五、数据治理和审计

1. 图片、导出文件、日志和 AI 原始响应均落在本地文件系统或数据库，没有对象存储、加密、保留期限、备份和恢复方案。

2. AI 检查结果保存了原始模型响应，需要明确敏感信息、医学内容和客户数据的存储与脱敏策略。

3. 题目删除是硬删除，关联审核记录和图片可能级联消失，不符合严格审计要求。

4. 当前审计日志主要记录动作描述，没有保存修改前后完整快照；部分日志写入错误被忽略，无法保证审计完整性。

5. 如果未来支持多个客户或机构，当前数据库没有租户/机构隔离字段；只能作为单客户独立部署。

## 六、性能和可靠性

1. 许多查询是“查出全部数据后在 Go 内存分页/筛选”，包括题目、知识点、统计和审核结果。
   - 影响：数据量增长后会导致响应变慢、内存上涨和数据库压力增大。

2. AI 生成和生图是同步 HTTP 请求，长时间占用连接。
   - 建议改为异步任务，提供任务状态、失败重试、取消和进度。

3. 没有健康检查、指标监控、结构化日志、告警、熔断和统一重试策略。

4. 启动时自动执行数据库结构和迁移，没有正式迁移版本、锁和回滚机制。

5. `start.sh` 依赖本机 Homebrew PostgreSQL、`nohup` 和 Vite 开发服务器，不是生产部署方案。

6. 题目保存使用 upsert，但没有乐观并发控制，多个用户同时编辑时可能静默覆盖。

## 七、产品和运维缺口

项目文档中已经明确但尚未完全补齐的内容包括：

- 知识点版本管理、导入历史和回滚
- 图片候选图对比和批量审核
- 生产部署手册
- 数据库备份、恢复和恢复演练
- 客户权限初始化和管理员交接
- 题目重复检测、敏感内容检测和质量基准集
- 模型升级回归测试
- 审核 SLA、超时提醒、任务转派和人员离职处理
- 导出水印、下载审计和正式题库发布回滚

此外，项目文档中的数据库数量存在不一致：README 写 12 张，进展文档写 16 张，当前 schema 实际定义 16 张表。发布文档和运行手册需要统一版本。

## 八、建议实施顺序

### 第一阶段：阻断风险 ✅（2026-08-20 已完成）

1. ✅ 删除默认生产账号和默认密钥，增加密钥强制校验
   - JWT_SECRET 为默认值时 serve 拒绝启动；默认 admin 仅空库创建且密码随机生成（或 AIGO_ADMIN_PASSWORD 指定）
2. ✅ 统一所有题目、题库、图片和审核接口的权限范围校验
   - 详情/编辑/删除/发布/图片/AI检查/导出/审核任务与记录全部按题库范围校验，越权 403
3. ✅ 增加请求体、上传文件、图片下载和响应大小限制
   - 请求体 10MB 上限（MaxBytesReader），图片下载需认证
4. ✅ 收紧 CORS，使用 HTTPS 和生产级反向代理
   - CORS 改白名单（空=禁止跨域）；HTTPS/反代留给部署手册（第三阶段）
5. ✅ 明确唯一的审核通过和发布流程
   - 现状为最终决断直接入库（PublishQuestion 仍保留作兜底，已在第二阶段的发布校验中收紧）

**待办（随第二阶段处理）**：图片目录公开静态服务已加认证；剩余为并发与版本快照等，见第二阶段。

### 第二阶段：审核可信性

1. 增加题目版本快照、差异比较和恢复。
2. 为审核投票增加数据库事务、行锁或乐观锁。
3. 将题目状态、审核任务、审核记录的关键变更放入同一事务。
4. 增加图片审核通过后的正式绑定机制。
5. 增加批量任务和批量导入的幂等控制。

### 第三阶段：生产可靠性

1. 将 AI 生成、生图和批量导入全部任务化。
2. 将列表、搜索、统计改为数据库分页和筛选。
3. 增加健康检查、监控指标、告警、结构化日志和成本统计。
4. 建立数据库迁移、备份、恢复和回滚流程。
5. 提供 Docker 或正式服务编排、部署和升级方案。

### 第四阶段：医学质量治理

1. 建立专家验收标准和人工复核责任制度。
2. 建立固定医学题目质量基准集。
3. 对模型、prompt、题目重复率、答案唯一性、医学科学性进行回归测试。
4. 将 AI 定位为辅助工具，禁止未经人工审核的内容进入正式题库。

## 九、最终判断

当前版本可以继续用于：

- 内部流程演示
- 小规模试用
- 专家审核流程验证
- AI 生成质量实验

当前版本不适合直接用于：

- 真实客户生产环境
- 多机构共享部署
- 正式考试题库管理
- 无人工复核的医学内容发布
