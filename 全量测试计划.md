# AIgo 全量测试计划

> 版本：v1.0（2026-09-13）
> 基准：`功能需求文档.txt` + `AGENTS.md` + `docs/guides/项目进展指引.md`
> 覆盖代码基线：当前分支 `HEAD`（每次验收需以 `git rev-parse HEAD` 记录，并核对本地 / GitHub / ECS 三处一致）
> 用途：验收测试与回归测试的总纲。每个模块给出用例清单（正常 / 边界 / 异常 / 越权）、
> 验收标准与覆盖方式（自动化 / 网页模拟 / 手工）。

---

## 1. 测试环境与分层

| 层 | 名称 | 载体 | 触发时机 |
|---|---|---|---|
| L1 | 单元与集成测试 | `go test ./...`（含 `AIGO_TEST_POSTGRES_DSN` 真实 PG 集成、`-race`） | 每次提交（GitHub Actions） |
| L2 | 前端状态测试 | `node --test web/src/*.test.js` + `npm run build` | 每次提交（CI） |
| L3 | 浏览器 E2E | `tools/e2e/*.py`（Playwright + Chrome，本地栈） | 每次功能迭代后 / 验收前 |
| L4 | 手工验收 | 浏览器按本文档用例逐条执行 | 验收阶段 |
| L5 | 部署与运维 | Docker Compose / 备份恢复演练 | 部署与周期演练 |

环境：
- 本地：`./start.sh`（PG@16 + 后端 8080 + Vite 5173），演示账号 admin/admin、expert/expert、expert2/expert2、teacher/teacher；
- 服务器：`http://123.56.164.1`（Caddy + Compose，迁移容器 + 应用 Healthy）。

**准入**：L1/L2 全绿且 `govulncheck` 可达漏洞为 0。
**准出**：本文档所有 P0/P1 用例通过，遗留缺陷不涉及数据一致性与权限边界。

账号矩阵（关键）：超级管理员 admin；下放管理员（可配 view_all 与否两种形态）；expert（命题+审核+退修+分享）；teacher（仅命题）；零权限新注册用户。

---

## 2. 模块用例

### M1 认证与账号安全

| 编号 | 场景 | 预期 |
|---|---|---|
| M1-01 | 正确账密登录 | 200 + token，跳转命题工作区 |
| M1-02 | 错误密码连续 7 次 | 每次统一 401（不区分"用户不存在/密码错"） |
| M1-03 | 同账号第 8 次失败 / 同 IP 第 30 次失败 | 429 限速；失败审计仅存账号/IP 短哈希 |
| M1-04 | 不存在账号登录 | dummy bcrypt 恒时响应，响应与 M1-02 不可区分 |
| M1-05 | 自助注册（开关开） | 201；角色/直接权限/题库范围全空；管理员用户列表自动出现 |
| M1-06 | 注册开关关闭（AIGO_REGISTER_ENABLED=0） | 登录页隐藏入口；接口 403 |
| M1-07 | 修改密码 | 旧密码立即失效；新密码可登录 |
| M1-08 | 管理员禁用账号 | 该账号既有 token 立即失效（每请求校验启用状态） |
| M1-09 | 超管保护 | admin 角色行不可通过用户管理创建第二个 super_admin；超管不可自禁/自删 |
| M1-10 | JWT 过期/伪造 | 401；受保护接口全部拒绝 |
| M1-11 | 未认证访问任意 /api 业务接口 | 401（除 /health、登录、注册、注册开关） |

### M2 权限与角色（RBAC × 题库范围 × 三档可见性）

| 编号 | 场景 | 预期 |
|---|---|---|
| M2-01 | 25 个权限点矩阵渲染与勾选 | 权限矩阵完整展示；勾选后既有 Token 实时生效 |
| M2-02 | 默认可见性（无 view_all、bank_ids 空） | 仅本人题目（三层），他人题详情 403 |
| M2-03 | 授予 question:view_all | 立即可见所有用户题目与完整子题库目录 |
| M2-04 | 直接分配题库范围（bank_ids） | 仅范围内题库的题目可见 |
| M2-05 | 角色模板权限 + 用户级 bank_ids 叠加 | 角色给能力、bank_ids 收边界（角色不可绕过范围） |
| M2-06 | question:view / view_formal / view_eliminated 三层独立授权 | 任一层未授权则该层 403，无旁路 |
| M2-07 | review:view_results 独立 | 个人题库查看权不得旁路审核记录汇总 |
| M2-08 | 子题库目录收敛 | 普通用户仅见本人题目涉及的子题库 |
| M2-09 | 移除 view_all | 实时收回全量可见性（既有 Token 生效） |
| M2-10 | 全局题库入口（question:view_global） | 无权限者不显示"全局题库"切换，接口 403 |
| M2-11 | 角色模板 CRUD | 仅 super_admin 可改模板；admin 角色被自愈保护（缺项补齐、role:manage 移除、view_all 不默认下发） |
| M2-12 | 每个业务接口 × 每类角色 | 按"权限点 → 角色 → 预期 200/403"矩阵全组合抽查（送审=user:manage 等） |

### M3 知识点与年度大纲版本

| 编号 | 场景 | 预期 |
|---|---|---|
| M3-01 | Excel / UTF-8 CSV / Word 表格批量导入（多文件 ≤20） | 全部校验通过后一个事务写入；返回新增/更新/跳过统计 |
| M3-02 | 同文件重复导入 | 内容一致全部跳过；有变化更新 |
| M3-03 | 单个知识点新增/编辑/删除 | 同版本大纲代码唯一；编辑校验修订号（并发互不覆盖） |
| M3-04 | 新建版本（草稿）→ 启用 | 默认展示年份最新已启用版；草稿不可用于出题 |
| M3-05 | 整套删除版本 | 原子删除；历史题目/审核记录/任务快照保留；过期导入/编辑请求不能恢复已删版本 |
| M3-06 | 删除默认版本 | 回退剩余最新已启用版；无已启用版显示空态，不自动启用草稿 |
| M3-07 | 目录树逐级展开 + 点击父目录 | 展示该目录全部后代知识点 |
| M3-08 | 关键词/目录过滤/分页 | 全部限定当前版本范围 |
| M3-09 | 出题与批量任务的知识点快照 | 修改/删除知识点不影响已有题目与已提交任务 |
| M3-10 | 导出当前版本 Excel | 可再导入；导出-导入往返无损 |
| M3-11 | 列表/树/元数据接口性能 | 快照缓存命中后每请求 ≤50ms（7200 点规模） |
| M3-12 | 普通已登录用户访问知识点搜索 | 无需 knowledge:manage；新增/编辑/删除/导入等写操作仍需权限 |

### M4 AI 单题生成（持久任务 + AI 检查）

| 编号 | 场景 | 预期 |
|---|---|---|
| M4-01 | 提交生成 | 立即返回 `pending` 运行（无题目），不阻塞 HTTP |
| M4-02 | 前端表现 | "任务已提交"→轮询→完成展示题目；**任何中间状态不得显示"已生成 0 道题"** |
| M4-03 | 同 run_id 重复提交 | 幂等返回同一运行，不重复执行 |
| M4-04 | 刷新页面（生成中） | 恢复流程正确展示进度与已完成结果 |
| M4-05 | worker 执行 | 题目以 ai_draft 落库、owner=提交人、按专业自动归纳（受限账号归入其可见题库） |
| M4-06 | LLM 瞬时失败 | 按退避重试（默认 3 次）后成功或耗尽 failed |
| M4-07 | 落库阶段失败 | 直接终态，不重试（防重复题目） |
| M4-08 | 进程重启（生成中） | 新进程 worker 接管 pending/租约过期运行 |
| M4-09 | AI 检查通过 | ai_draft → ai_reviewed；淘汰则物理删除 + 档案在生成页展示 |
| M4-10 | 元数据不一致（难度/认知层次等） | 仅 info 反馈，不得单独淘汰 |
| M4-11 | AI 检查任务队列 | 抢占互斥、租约回收、退避重试、耗尽标记、重启恢复 |
| M4-12 | AI 检查落库原子性 | 结果/题目状态/淘汰删除/任务完成同事务，逐步故障回滚 |
| M4-13 | 送审前置（RequireAICheck 开） | 未通过 AI 检查（非 ai_reviewed）不可送审 |
| M4-14 | 生成权限 | 无 question:generate 者 403；界面无生成入口 |
| M4-15 | 受限账号生成 | 无需选择目标题库（选择器已移除），自动归入其可见题库 |

### M5 批量推理（暂缓项，仅验证不回归）

| 编号 | 场景 | 预期 |
|---|---|---|
| M5-01 | Web 批量页（batch:run） | 模拟执行器：提交/进度/导入幂等流程可走通，不外发 API |
| M5-02 | 无 batch:run | 侧栏无入口；直接访问 `/generate/batch` 回单题页并提示 |
| M5-03 | CLI batch-run/status/download | DashScope 兼容路径不受影响（不实际外发） |

### M6 审核流程（送审 → 投票 → 决断 → 终态）

| 编号 | 场景 | 预期 |
|---|---|---|
| M6-01 | 管理员批量送审（明确子题库） | submission_bank_id 固化快照；自动匹配审题人（直接权限+范围） |
| M6-02 | 送审资格 | 仅 user:manage；审题人/出题人 403 |
| M6-03 | 逐轮投票 | 达通过票数即过轮；反对票留痕；全员审完有反对 → conflict |
| M6-04 | 一票否决（veto 流程） | 任一驳回直接驳回题目 |
| M6-05 | 同轮隔离 | 非把关人看不到他人实时票数与评语 |
| M6-06 | 待我审核 | 仅当前轮分配给我且未投票的任务；投完移开、下一轮再出现 |
| M6-07 | 待我决断 | conflict + 我在把关人名单（名单空=任意把关人；系统管理员全部可见） |
| M6-08 | 把关人决断 | approved（最终轮）→ published；rejected → 终态；revision_required → 任务退修、题目回 ai_reviewed |
| M6-09 | 决断评语校验 | 退回修改/驳回必须至少一栏理由 |
| M6-10 | 待我修改 | 仅退回给本人（created_by）的 revision_required 任务；可编辑并提交回库（版本+1，无 AI 复检） |
| M6-11 | 管理员重新送审 | 退修题恢复 reviewing、本轮票清零、attempt+1 |
| M6-12 | 撤销送审 | 未完成任务删除、题目恢复提交前状态；终态任务保留 |
| M6-13 | 流程编辑保护 | 有进行中任务禁改；有历史任务禁删（保留追溯） |
| M6-14 | 版本绑定 | 投票/决断时题目版本不一致 → 拒绝 |
| M6-15 | 并发一致性 | 并发投票/重复送审/双实例写入无丢票（advisory lock + 事务，已有竞态测试） |
| M6-16 | 终态锁 | published/rejected 禁止再审核、再编辑（published 仅可撤回后修订） |
| M6-17 | 三个待办页在 SQL 预过滤后 | 列表内容与过滤语义和重构前一致（页面渲染、计数、排序） |

### M7 题库分层、可见性与删除

| 编号 | 场景 | 预期 |
|---|---|---|
| M7-01 | 三层视图（正式/待审核/淘汰） | 各层独立授权；默认收敛为可见分层并集 |
| M7-02 | 统一搜索 | 关键词覆盖题干/选项/答案/解析/考核要点/专业/系统/知识点快照/大纲代码/来源/ID；空格与中英文逗号分号分词、AND 匹配；`%`/`_` 按字面 |
| M7-03 | 难度筛选 | 兼容数值系数与文本（≤0.60 简单 / 0.61-0.80 中等 / >0.80 困难） |
| M7-04 | 搜索竞态 | 旧请求晚返回不得覆盖新请求 |
| M7-05 | 手动添加进子题库 | 仅可选待审核层可调整分类的题；已在库中的命中项标记"已在本库"；筛选在后端分页前执行 |
| M7-06 | 删除（published/有审核史） | 归档：进本人淘汰题库、标注"已归档"、版本/审核记录/审计保留 |
| M7-07 | 删除（无审核史草稿） | 物理删除 |
| M7-08 | 删除守卫 | 审核中/待决断 409；已淘汰终态 409；已提交分享 409 |
| M7-09 | 发布（review:final） | published 唯一成功终态；把关人名单校验 |
| M7-10 | 撤回（user:manage） | published → ai_reviewed；有分享申请时禁止 |
| M7-11 | 归档题恢复（未来项） | 当前需管理员数据库操作；界面无恢复入口（记录为待办） |
| M7-12 | 题库页子题库筛选 | 已移除（子题库只在题库管理页与送审流程） |
| M7-13 | 题目详情弹窗 | 题干/选项/答案/解析/大纲代码/知识点/元信息完整；不出题人 |

### M8 全局题库与分享

| 编号 | 场景 | 预期 |
|---|---|---|
| M8-01 | 单题分享（本人 published + question:share） | pending 进全局待审核；一题仅一次，重复 409 |
| M8-02 | 非本人题分享 | 拒绝（单题与批量预览均 OwnerID 限定） |
| M8-03 | 批量分享预览/提交 | 已申请过自动跳过；每批 ≤500；确认数量与范围 |
| M8-04 | 分享审批（share_review） | approved → 全局正式；rejected → 全局淘汰（原题与审核记录保留） |
| M8-05 | 全局三层视图 | 按分享状态推导；需 question:view_global；全局题不出题人 |
| M8-06 | 分享审批页 | 管理员可见申请人（owner_name，设计内唯一例外） |
| M8-07 | 全局题操作边界 | 全局正式/淘汰题不可再调整子题库、不可编辑 |
| M8-08 | 分享中的题 | 不可撤回、不可删除 |

### M9 统计

| 编号 | 场景 | 预期 |
|---|---|---|
| M9-01 | 我的/全局双范围 | 个人统计按 owner、全局按分享状态聚合，互不混入 |
| M9-02 | 范围权限 | stats:view + 题库范围收敛；无 view_global 不得看全局统计 |
| M9-03 | 分布与趋势 | 状态/难度/专业/子题库分布、90 天创建趋势（存储端聚合） |
| M9-04 | 知识点覆盖与大纲统计 | 默认版本统计 + 各已发布版本总数 |
| M9-05 | AI 检查与任务指标 | verdict 分布、淘汰数、任务排队计数、generation_runs 状态计数 |

### M10 审计日志

| 编号 | 场景 | 预期 |
|---|---|---|
| M10-01 | 关键操作留痕 | 生成/送审/投票/决断/发布/撤回/归档/删除/退修/分享/登录失败/流程变更/用户变更 |
| M10-02 | 题库操作审计 | bank_create/bank_update/bank_delete（本次新增） |
| M10-03 | 审计与业务同事务 | 关键配置操作审计失败 → 业务一起回滚（故障注入） |
| M10-04 | 日志查询 | 按题目/操作人筛选（audit:view） |
| M10-05 | 脱敏 | 登录失败审计不含完整用户名/IP/密码/Token |

### M11 导出

| 编号 | 场景 | 预期 |
|---|---|---|
| M11-01 | Excel/Word 导出（question:download） | 仅 published；跨页勾选/全选筛选结果；记录导出审计 |
| M11-02 | 列完整性 | 题号/题干/A-E/答案/说明/大纲代码/难度/认知/考核要点/专业/系统；不输出命题人列 |
| M11-03 | 命题人信息移除 | Excel/Word 均不得出现“命题人”列、机构占位符或“朝阳医院AI”字样 |
| M11-04 | 下载鉴权 | 下载文件需认证；无 question:download 403 |
| M11-05 | 文件有效性 | Excel 可再导入；Word（OOXML）无 pandoc 依赖；特殊字符转义正确 |

### M12 AI 服务配置

| 编号 | 场景 | 预期 |
|---|---|---|
| M12-01 | 仅超管可见可改 | 其他角色（含下放管理员）403；前端无入口 |
| M12-02 | API Key 加密落库 | 接口只返回脱敏值；库中无明文 |
| M12-03 | 生成/检查模型分离 | 可分别配置；本地/云端切换（QWEN_DEPLOYMENT） |
| M12-04 | 动态切换 | 激活配置即时生效，无需重启 |
| M12-05 | 无配置兜底 | 环境变量兼容导入；无任何配置时生成/检查明确报错 |

### M13 系统管理（用户/角色/历史兼容）

| 编号 | 场景 | 预期 |
|---|---|---|
| M13-01 | 用户 CRUD + 权限矩阵 + 题库范围 | 变更实时生效；新注册零权限 |
| M13-02 | 角色模板 | super_admin 只读唯一；admin 可调业务权限；自定义角色 n 个 |
| M13-03 | 专家库历史接口 | /api/experts 列表兼容（无独立页面） |
| M13-04 | 操作日志页 | 动作类型/actor 昵称映射完整（含 bank_*、archive、submit_bank 等新动作） |
| M13-05 | 我的工作台 | 所有已登录用户可访问；待我审核/决断/修改/分享审批数量与对应接口一致；本人三层题库数量按 owner 隔离 |
| M13-06 | 个人设置 | 显示当前用户名/角色/权限数；昵称即时更新侧栏；密码不一致前端拦截，修改后旧密码失效 |
| M13-07 | 个人中心导航 | 左侧独立首组，仅含工作台/设置；登录和越权回退到工作台；零权限用户不出现业务入口 |

### M14 非功能、安全与部署

| 编号 | 场景 | 预期 |
|---|---|---|
| M14-01 | 请求体上限 | 10MB（导入 32MB），超限 400/413 |
| M14-02 | CORS 白名单 | 非白名单跨域拒绝 |
| M14-03 | SQL/路径注入 | 关键词按字面匹配；文件路径穿越拒绝 |
| M14-04 | 单题生成目标 <1 分钟；查询全部分页 | 性能抽样 |
| M14-05 | 并发与竞态 | 并发投票、重复送审、双实例、`-race` 全量（已有测试） |
| M14-06 | 数据库迁移 | 校验和一致；未迁移/断档/过新/篡改拒绝运行；事务回滚与并发安全 |
| M14-07 | 健康检查 | /health/live 进程存活；/health/ready 2s 内验证 PG 与关键 Schema，不泄露 DSN |
| M14-08 | 备份恢复 | create/verify/drill/restore/导出；篡改拒绝；禁覆盖业务库（周期演练） |
| M14-09 | 异机备份 | age 加密 → S3 不可覆盖上传 → 回读校验 → 离线恢复闭环（上线前验收） |
| M14-10 | 容器部署 | 非 root 只读根、Secret 文件挂载、80/443 发布、迁移一次性服务 |
| M14-11 | 两地一致性 | 本地 / GitHub（CI 绿）/ 服务器 同一提交；关键文件校验和一致；服务器不自动跟随 Git |
| M14-12 | 静态托管 | 深层路由刷新 200；/.env 与缺失资源 404；HTML 禁强缓存 |
| M14-13 | 窄屏前端体验 | 390px 移动端核心页面无横向溢出；题库搜索、批量操作、分页仍可见且可操作；表格改为可读卡片 |

---

## 3. 已固化自动化与本计划的映射

| 自动化资产 | 覆盖本文档 |
|---|---|
| GitHub Actions（Go 全量+PG 集成+竞态+govulncheck；前端构建+测试） | M1-M13 的 L1 层回归 |
| `tools/e2e/visibility_e2e.py` | M2-02/03/08、M7 三档可见性抽样 |
| `tools/e2e/full_regression.py` | M4-01/02/03/04/09、M3-07/08、M7-12/13 抽样 |
| `tools/e2e/review_todo_e2e.py` | M6-06/07/08/10 抽样 |
| `tools/e2e/archive_deletion_e2e.py` | M7-06/08 抽样 |
| `tools/e2e/export_e2e.py` | M11-01/02/03/04/05 |
| `tools/e2e/personal_center_e2e.py` | M1-01/05/07、M13-01/05/06/07、M14-13 |
| review/storage/api 包集成测试（含故障注入） | M6-15、M4-11/12、M10-03、M2 全矩阵 |

### 3.1 ECS 真实业务链路 UAT 记录（2026-09-13）

- 使用实际 ECS 网站登录；管理员前端创建临时专家账号、临时分类题库和十轮审核流程，仅生成 1 道题。
- 从生成、异步质量检查、题库归类、批量送审开始，临时专家账号在「待我审核」页面逐轮通过 10 轮；管理员在「待我决断」查看十轮评语/时间线并最终通过，题目进入 `published/已通过`。
- 在审核结果、数据统计、操作日志页面核对结果；从个人正式题库发起分享，在分享审批页批准，并在全局正式题库检索确认。
- 验证分享后的正式题目删除保护返回 409；测试账号、题目、流程、分类题库、生成运行记录、审计记录和临时限速记录均按随机 ID 精确清理，既有业务数据未修改。
- 桌面端与 390px 移动端共 13 个核心页面复测通过，无控制台/页面/请求错误、无横向溢出；题库页搜索栏、批量操作栏和分页栏均保持可用。此前发现的流程、审核结果、统计、日志、用户管理页窄屏溢出已修复。

## 4. 执行节奏

1. **每次提交**：CI（L1+L2）必须绿；
2. **每次功能合并到部署分支**：`tools/e2e` 相关脚本本地全绿后同步服务器；
3. **每次服务器部署**：migrate exit=0 → /health/ready 200 → 公网登录页 200 → 冒烟（登录+题库+一个核心流）；
4. **验收**：按本文档 L4 全量走查一遍（建议按 M1→M13 顺序，双人交叉）；
5. **周期**：M14-08 备份恢复演练每月一次；govulncheck 每周。

## 5. 明确不在范围

- AI 生图/多模态（永久不做）；真实批量推理执行器（暂缓，仅验证模拟流程不回归）；
- 多租户/共享数据库；Neo4j/知识图谱/IRT/Bloom；
- 服务器域名/HTTPS（待客户提供域名后另行验收）。

## 6. 缺陷分级

- **P0 数据一致性/权限越权/安全**：阻塞发布（历史教训：pending 状态前端误判、子题库目录泄露均属此类）；
- **P1 功能错误但有绕行**：限期修复；
- **P2 体验/文案**：排期。
