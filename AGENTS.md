# Codex 必读：AIgo 当前开发约束

## 1. 必须先读

后续开发必须以根目录 `功能需求文档.txt` 为产品基准。

如果本文与 `功能需求文档.txt` 冲突，以 `功能需求文档.txt` 为准。

## 2. 当前项目定位

AIgo 当前版本是面向国家执业医师考试 A2 型试题的 AI 辅助命题系统。

当前主流程：

```text
知识点 Excel / CSV / Word 表格
        ↓
千问生成 A2 试题草稿（题目唯一来源是 AI 生成，无手动新建）
        ↓
AI 质量检查（落库后异步执行，仅首次生成时检查一次）
        ├─ 不通过 → 自动删除不入题库，淘汰原因留档并在生成页展示
        └─ 通过 → ai_reviewed（题库可见）
        ↓
多轮专家审核与决断
		├─ 需修改 → 题目恢复 ai_reviewed，退回生成者（「待我修改」页），必须保存新版本后按原流程从第一轮重新送审，无需 AI 复检
        ├─ 驳回 → rejected 终态锁定：禁止修改与重新提交
        └─ 通过 → published 定稿（管理员可在题库「撤回」回 ai_reviewed 修订）
        ↓
正式题库
```

当前重点是做一个可落地、可审核、可裁剪的系统，不要扩展成复杂研究平台。

## 3. 技术路线

- 主系统优先使用 Go。
- Python/conda 只作为后续算法实验的辅助方案。
- 文本出题直接调用阿里云百炼千问 API。
- 当前不接知识图谱。
- 当前不强依赖历史真题。
- 当前不要求解析和试题来源为必填字段。

千问相关环境变量：

- `DASHSCOPE_API_KEY`
- `QWEN_BASE_URL`
- `QWEN_MODEL`

不得把真实 API Key 写入代码或文档。

## 4. 当前必须实现或保留的方向

### 4.1 知识点管理

按年度考试大纲管理整套知识点（2026-09-04 用户明确更新需求）。

- 每个大纲版本独立存储；允许不同版本复用大纲代码，知识点 ID 必须跨版本唯一。
- 新版本先整理、再启用；默认展示年份最新的已启用版本，历史版本仍可出题。
- 左上显示当前版本，右侧打开独立版本窗口进行搜索、切换和整套删除。
- 整套删除必须保证原子性；历史题目、审核记录和已提交批量任务快照保留。
- 删除默认或最后一个版本后处理回退与空状态，不能自动启用草稿；过期的导入/编辑请求不能恢复已删除版本。
- 支持多文件 Excel / CSV / Word 表格批量导入和单个知识点新增、编辑、删除。
- 左侧目录树按分类、专业/系统、单元、细目组织；点击父目录展示全部后代内容。
- 修改、删除、整套替换仅影响当前版本；已有题目和批量任务保留出题时的知识点快照。
- 搜索、统计、出题、批量去重须明确版本范围；批量结果必须使用任务提交时的快照。
- 其他知识点选择界面保留现有布局，仅适配版本选择和关联参数。
- 不做知识点与指南、教材、文献的复杂关联，也不维护知识图谱关系。

### 4.2 AI 出题与质量检查

题目唯一来源是 AI 生成（单题 / 批量推理）；没有手动直接出题，所谓"手动"仅指对 AI 生成结果的修订，且修订窗口仅限"专家审核退回修改"（待我修改页）。生成页对 AI 产出为纯展示。

AI 质量检查约束（2026-09-06 用户明确）：

- 出题落库后自动异步检查，仅在首次生成时执行一次；任何人工修改都不触发复检。
- 检查不通过的题目自动淘汰删除（不入题库），淘汰原因留档并在生成页展示"本次生题具体情况"。
- 检查通过（ai_reviewed）是送审的不可关闭强制前置。
- 检查任务持久化（ai_check_tasks 表）：每题只创建一次任务，任务内部超时重试，耗尽后标记最终故障并阻断流程；禁止人工复检、强制通过和批量补查。

题目首期核心字段：

- 题干。
- 选项。
- 正确答案。
- 知识点。
- 难度。
- 审核状态。
- 版本记录。

解析和试题来源是可选字段，首期不要作为必填项，也不要在质量校验里强制要求。

### 4.3 多轮审核

审核流程必须可配置。

需要支持：

- 专家库。
- 审核流程配置。
- 审核轮数配置。
- 每轮审核人或专家组配置。
- 审核意见记录。
- 修改记录。
- 通过、驳回、退回修改。

结果语义（2026-09-06 用户明确，驳回与需修改不重复）：

- 需修改：题目恢复 AI 检查通过状态，自动进入生成者的「待我修改」页；必须保存产生新版本，再按该题原审核流程从第一轮重新送审，无需 AI 复检。支持单题与批量重送。
- 驳回：终态锁定，禁止修改与重新提交。
- 通过：全轮通过并经把关人决断后入库（published）定稿；管理员在题库「撤回」后，题目进入原生成者「待我修改」，保存新版本后按原流程从第一轮重审。

### 4.4 题库分层（2026-09-06 用户明确）

题目按生命周期分属三个逻辑题库（`domain.QuestionTier`，由状态经 `TierForStatus` 唯一推导，不单独落库）：

- **正式题库（formal）**：published 定稿题。"入库"即指进入正式题库，唯一入口是审核决断通过。
- **待审核题库（working）**：生成 → AI 检查 → 审核 → 退回修改全流程中的题目。
- **淘汰题库（eliminated）**：rejected 终态锁定与 archived 留档。AI 检查不通过的题目物理删除并留淘汰档案，不进任何库。

约束：

- 正式题库密封：其他功能（分类子题库、审核分配、数据分析等）不得再操作正式题库；仅允许自动入库与权限者删除/撤回。
- 分层查看权限独立授权：`question:view`（过程）、`question:view_formal`（正式）、`question:view_eliminated`（淘汰）；删除正式题库题目需 `question:delete` + `question:delete_formal`。
- 导出是正式题库唯一文档出口：仅 published 可导出，且调用者需 `question:download` + `question:view_formal`。
- 分类子题库（bank 服务）只作用于待审核题库；Collect/加入/移出/批量归类对 formal/eliminated 题目一律拒绝。
- 管理员的「撤回」（unpublish）保留：published → ai_reviewed，题目由正式题库回流待审核题库。

## 5. 当前不要做的内容

以下内容目前不要实现：

- 手动直接出题（题目唯一来源是 AI 生成；修订仅走"待我修改"窗口）。
- 题目附带图片、多模态素材、AI 生图及图片审核。
- 知识图谱。
- Neo4j。
- 混淆网络。
- IRT 或深度 IRT。
- Bloom 分类。
- 强化学习优化。
- 医学模型微调。
- 复杂来源资料管理。
- 历史真题强依赖。
- 把解析或试题来源做成必填。

如果后续需要使用历史真题，只能先做成轻量参考加载：

```text
选择知识点
        ↓
检索少量相关历史真题或样例
        ↓
加载到提示词上下文
        ↓
千问参考样例生成新题
```

不要为了历史真题提前设计复杂知识库或知识图谱。

## 6. 代码风格约束

- 保持 Go 为主语言。
- 优先使用标准库，除非收益明确。
- 新增依赖前必须说明原因。
- 不要大规模重构当前骨架。
- 不要把审核流程写死在代码里。
- 不要把知识点管理做复杂。
- AI 生成结果默认是草稿。
- 未通过审核的题目不能进入正式题库。
- 测试命令优先使用 `go test ./...`。

## 7. 当前工程骨架

### 后端 Go

- `cmd/aigo/main.go`：CLI 入口，支持 serve/import/generate 等命令。
- `internal/config/config.go`：从 .env 文件和环境变量加载配置。
- `internal/llm/`：千问 OpenAI 兼容客户端。
- `internal/domain/`：题目、知识点、审核等领域类型。
- `internal/generator/`：千问出题服务（prompt 构建 + JSON 解析）。
- `internal/evaluator/`：规则评估（选项数、答案、解析）。
- `internal/knowledge/`：知识点服务（导入、搜索、筛选）。
- `internal/importer/`：xlsx 导入器（题目 + 知识点）。
- `internal/review/`：审核服务（多轮流程、状态流转、待我修改）。
- `internal/aicheck/`：AI 质量检查服务（LLM 检查 + 持久化任务队列 + 淘汰记录）。
- `internal/audit/`：审计日志服务。
- `internal/auth/`：JWT 认证 + RBAC 权限。
- `internal/api/`：HTTP REST API（handler 按资源拆分）。
- `internal/pipeline/`：业务编排层，串联各服务。
- `internal/storage/interfaces.go`：所有存储接口定义。
- `internal/storage/postgres/`：PostgreSQL 实现（schema.sql + store.go）。
- `internal/storage/testutil/`：内存实现（仅供测试）。

### 前端 Vue 3

- `web/src/App.vue`：根组件（侧边栏、用户信息、个人设置入口）。
- `web/src/api.js`：API 服务层（封装所有后端接口）。
- `web/src/auth.js`：认证状态管理（token、用户信息）。
- `web/src/router.js`：路由配置 + 登录守卫。
- `web/src/pages/`：20 个页面组件（含我的工作台、个人设置、待我修改和各业务管理页）。

### 配置文件

- `.env`：环境变量（API Key、数据库连接），不提交 git。
- `configs/example.env`：环境变量示例。
- `configs/review_flows.json`：审核流程配置。
- `deploy/`：生产部署编排（Compose + Caddy + 备份），使用指引见 `deploy/README.md`。

## 8. 线上服务器与 Caddy（改动前必读）

线上唯一生产环境：阿里云 ECS `123.56.164.1`，域名 `imaichika.love` / `www.imaichika.love`（Caddy 自动 HTTPS），部署在服务器 `/opt/aigo`（docker compose）。

- Caddy 站点地址由服务器 `/opt/aigo/deploy/production.env` 的 `AIGO_SITE_ADDRESS` 控制；当前权威值、修改流程与验收命令见 `deploy/README.md` 第 10 节，凡涉及 Caddy、域名或生产 env 的改动前必读。
- 两个禁忌：站点地址不能丢域名（域名请求会落入 Caddyfile 兜底块返回 421，症状为"网站打不开"）；IP 项必须保留 `http://` 前缀（裸 IP 会被 Caddy 308 重定向到自签名 HTTPS，浏览器告警）。
- 修改 `AIGO_SITE_ADDRESS` 后必须 `./deploy/compose.sh up -d caddy` 重建容器，restart 不生效。
- 服务器 `/opt/aigo/deploy/production.env` 与 `deploy/secrets/` 是唯一权威源，Mac 侧副本不得反向覆盖；服务器登录凭证不入仓库。

后续开发应围绕 `功能需求文档.txt` 的树状图继续，不要主动增加无关模块。
